---
context: default
phase: null
status: paused
last_updated: 2026-06-09T05:31:53Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_Nenhum constraint de blocking identificado._

<current_state>
Integração TOConline completa implementada de forma ad-hoc (fora de fase GSD formal) na branch `refatoracao-v2`.
Build Vite passa sem erros. Testado parcialmente no browser — erros 401/500 foram corrigidos.
Último erro corrigido: `valorHora.toFixed is not a function` → `Number(cliente?.valorHora ?? 0)`.
TODO: commit + teste real do fluxo completo.
</current_state>

<completed_work>

### Novos ficheiros criados:
- `api/toconline/_fetch.js` — helper partilhado (tocFetch, fetchAllPages, obterUrlPdf)
- `api/toconline/status.js` — GET verifica token
- `api/toconline/clientes.js` — GET /api/customers + POST criar cliente
- `api/toconline/fornecedores.js` — GET /api/suppliers
- `api/toconline/artigos.js` — GET /api/products + /api/services (paralelo) + POST
- `api/toconline/documento.js` — GET detalhe + DELETE anular
- `src/features/admin/TOConlineAdmin.jsx` — módulo principal 4 tabs
- `src/features/admin/toconline/TOConlineClientes.jsx`
- `src/features/admin/toconline/TOConlineArtigos.jsx`
- `src/features/admin/toconline/TOConlineRelatorios.jsx`
- `src/features/admin/toconline/CriarDocumentoModal.jsx` — criação manual com autocomplete
- `src/features/admin/toconline/FaturarClienteModal.jsx` — wizard automático (cliente+período→fatura)

### Ficheiros modificados:
- `api/toconline/create-fatura.js` — usa _fetch.js, 12 tipos documento, suporte item_id
- `api/toconline/import.js` — usa _fetch.js (paths /v1/... mantidos, a verificar)
- `src/features/admin/AdminSidebar.jsx` — item TOConline no menu com subtabs
- `src/features/admin/AdminDashboard.jsx` — rota `activeTab === 'toconline'`
- `vercel.json` — maxDuration para 7 endpoints TOConline
</completed_work>

<remaining_work>

1. **Commit** todo o trabalho desta sessão
2. **Testar fluxo real**: ligar OAuth → tab Clientes → tab Artigos → botão "Faturar Cliente"
3. **Verificar import.js**: paths `/v1/commercial_sales_documents` vs `/api/v1/...` — se import der 500, mudar para `/api/v1/...`
4. **Migração Supabase**: confirmar se `supabase/migrations/20260608_toconline_integration.sql` já foi aplicada
</remaining_work>

<decisions_made>

- Endpoints corretos: `/api/customers`, `/api/suppliers`, `/api/products`, `/api/services` (confirmado via Swagger TOConline)
- Artigos = produtos + serviços em paralelo (dois endpoints separados na API TOConline)
- `FaturarClienteModal` usa dados locais do app (clients, logs, ajudas_faturadas_clientes) para pré-calcular — zero chamadas extra à API TOConline
- Bloquear render das tabs com estado `verificando` até `/api/toconline/status` responder — evita 401/500 em cascata
</decisions_made>

<blockers>

- **import.js paths**: usa `/v1/...` (herança do código original) mas swagger documenta `/api/v1/...`. Não testado. Se falhar, mudar 3 linhas em `api/toconline/import.js`.
</blockers>

## Required Reading (em ordem)
1. `api/toconline/_fetch.js` — entender o helper partilhado antes de qualquer modificação
2. `src/features/admin/TOConlineAdmin.jsx` — ponto de entrada do módulo UI
3. `src/features/admin/toconline/FaturarClienteModal.jsx` — lógica de faturação automática

## Infrastructure State
- Branch: `refatoracao-v2`
- Build: ✅ Vite build OK sem erros
- Env vars necessárias: `TOCONLINE_CLIENT_ID`, `TOCONLINE_CLIENT_SECRET`, `TOCONLINE_OAUTH_URL`, `TOCONLINE_API_URL`
- Migração pendente: `supabase/migrations/20260608_toconline_integration.sql`

<context>
Sessão focada em integração TOConline completa. Implementação foi iterativa — primeiro endpoints básicos, depois correção de erros (401→verificando, 500→paths corretos, toFixed→Number). Toda a lógica está funcional segundo o build. Falta apenas testar com ligação real ao TOConline.
</context>

<next_action>
1. `git add api/toconline/ src/features/admin/TOConlineAdmin.jsx src/features/admin/toconline/ src/features/admin/faturas/TOConlinePanel.jsx src/features/admin/AdminDashboard.jsx src/features/admin/AdminSidebar.jsx vercel.json`
2. `git commit -m "feat: integração completa TOConline — módulo admin, endpoints API, faturação automática"`
3. Testar no browser: Admin → TOConline → ligar → Clientes → Artigos → Faturar Cliente
</next_action>
