---
context: default
phase: null
status: paused
last_updated: 2026-05-20T20:19:37Z
---

# Continue Here — Faturação por Cliente + Pagamentos Bancários

<current_state>
Feature de associação pagamentos bancários ↔ faturação por cliente está completa no código e deployada (commit b8bfc3f). A tabela SQL ainda NÃO foi criada no Supabase — é o único bloqueador.
</current_state>

<completed_work>

- ✅ Migration SQL criada: `supabase/migrations/20260520_faturacao_clientes_pagamentos.sql`
- ✅ CostReports: coluna "Pago Banco" + "Estado" (PAGO/PARCIAL/PENDENTE) na tab Clientes + modal de link manual
- ✅ ReconciliacaoAdmin: botão 🔗 em créditos (matched + orphan_bank) + modal "Associar a Cliente"
- ✅ Auto-associação automática: ao processar extrato e ao selecionar run do histórico
- ✅ Período = mês anterior à data da transação (Maio → Abril)
- ✅ Matching robusto: tokens, sem sufixos legais (Lda, SA), sem pontuação, score ≥75%
- ✅ Botão "Auto" na barra de resultados da Reconciliação para re-executar
- ✅ Deployed: https://app-magnetic.vercel.app (commit b8bfc3f)
</completed_work>

<remaining_work>

1. **[BLOQUEANTE - HUMANO]** Aplicar migration no Supabase SQL Editor:
   - Copiar conteúdo de `supabase/migrations/20260520_faturacao_clientes_pagamentos.sql`
   - Colar e executar no SQL Editor do Supabase

2. **Deploy version.json** (menor):
   ```
   git add public/version.json
   git commit -m "chore: bump version"
   git push origin master
   vercel --prod
   ```

3. **Testar**:
   - Ir a Reconciliação → Histórico → selecionar extrato de Maio
   - Verificar se botão "Auto" associa automaticamente
   - Ir a CostReports → Clientes → selecionar Abril → verificar coluna Pago Banco
</remaining_work>

<decisions_made>

- Período da associação = mês anterior à data da transação (pagamento em Maio → billing de Abril)
- Matching por nome do cliente na descrição bancária (score ≥75% dos tokens significativos)
- Auto-associação corre automaticamente (sem modal) após processar ou selecionar run
- CostReports não precisa de alterações para o período — usa `selectedMonth` do UI
</decisions_made>

<blockers>

- **Tabela não existe no Supabase** — sem migration aplicada, todas as operações de link falham silenciosamente. O código faz `supabase.from('faturacao_clientes_pagamentos')` que retorna erro 404.
</blockers>

## Required Reading

1. `supabase/migrations/20260520_faturacao_clientes_pagamentos.sql` — migration a aplicar
2. `src/features/admin/CostReports.jsx` — função `carregarPagamentos`, `associarPagamento`, modal
3. `src/features/admin/ReconciliacaoAdmin.jsx` — funções `autoAssociarEntradas`, `matchClientByDesc`, `previousMonth`

## Infrastructure State

- **Vercel**: https://app-magnetic.vercel.app — código deployado, tabela em falta
- **Supabase**: tabela `faturacao_clientes_pagamentos` NÃO EXISTE ainda
- **Git**: branch master, último commit b8bfc3f, `public/version.json` não committed

<next_action>
Start with: Aplicar migration SQL no Supabase SQL Editor (conteúdo em `supabase/migrations/20260520_faturacao_clientes_pagamentos.sql`). Depois testar em produção.
</next_action>
