---
phase: 09-modularizacao-worker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/worker/contexts/WorkerContext.jsx
  - src/features/worker/WorkerDashboard.jsx
  - src/features/worker/index.js
  - src/app.jsx
autonomous: true
requirements:
  - WORKER-01

must_haves:
  truths:
    - "Worker can log in and see their dashboard"
    - "Dashboard displays current month hours and progress"
    - "WorkerContext manages local UI state (expanded days, inline editing, etc.)"
  artifacts:
    - path: "src/features/worker/contexts/WorkerContext.jsx"
      provides: "Local state management for worker dashboard"
      min_lines: 100
    - path: "src/features/worker/WorkerDashboard.jsx"
      provides: "Main worker dashboard component (stub)"
      min_lines: 50
    - path: "src/features/worker/index.js"
      provides: "Feature exports"
      exports: "WorkerDashboard, useWorker, WorkerProvider"
  key_links:
    - from: "src/features/worker/WorkerDashboard.jsx"
      to: "src/context/AppContext.jsx"
      via: "useApp() hook"
      pattern: "useApp"
    - from: "WorkerContext.jsx"
      to: "WorkerDashboard.jsx"
      via: "Provider + hook"
      pattern: "useWorker|WorkerProvider"
---

<objective>
Extract WorkerDashboard from app.jsx into `src/features/worker/` with proper context pattern. Create scaffolding that Phase 9 Plan 02 will populate with full functionality.

Purpose: Follow the proven Phase 7 admin modularization pattern for worker features.
Output: `src/features/worker/` directory with WorkerContext.jsx, WorkerDashboard.jsx (skeleton), and index.js.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-modularizacao-worker/09-RESEARCH.md
@src/features/admin/contexts/TeamContext.jsx (pattern reference)
@src/features/admin/TeamManager.jsx (pattern reference)
@src/features/admin/index.js (exports pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create worker feature directory structure</name>
  <files>src/features/worker/contexts/WorkerContext.jsx</files>
  <action>
Create directory structure and initial files:

1. Create `src/features/worker/` directory
2. Create `src/features/worker/contexts/` subdirectory

**File: src/features/worker/index.js**
```javascript
export { default as WorkerDashboard } from './WorkerDashboard';
export { useWorker, WorkerProvider } from './contexts/WorkerContext';
```

**File: src/features/worker/contexts/WorkerContext.jsx (SKELETON)**
Create the context file with:
- Import React, useState, useMemo, useEffect, useRef from 'react'
- Import from '../../context/AppContext' (useApp)
- Import from '../../utils/dateUtils' (toISODateLocal, isSameMonth, getLastBusinessDayOfMonth)
- Import from '../../utils/formatUtils' (formatHours, calculateDuration, calculateExpectedMonthlyHours, getScheduleForDay)
- Import icons from 'lucide-react' (Clock, ChevronLeft, ChevronRight, Calendar, CheckCircle, etc.)
- Create WorkerContext with useContext(React.createContext())
- Export useWorker hook that uses the context
- Create WorkerProvider component that:
  - Gets currentUser from useApp()
  - Gets logs, clients, schedules, personalSchedules, approvals from useApp()
  - Manages local state: inlineEditingDate, successMsg, inlineFormData, showSchedulesModal, showProgress, expandedDays, showPersonalBreaks, newPersonalForm, dismissedNotifs
  - Computes pendingApprovals using useMemo
  - Computes activeWorkerSchedule, expectedHours, daysList, monthLogs, todayHours, totalMonthHours
  - Computes currentMonthStr from currentMonth
  - Provides all values and handlers via context value
- Export WorkerProvider

The skeleton should have:
- All imports
- Context creation: `const WorkerContext = React.createContext(null);`
- useWorker hook: `export const useWorker = () => useContext(WorkerContext);`
- WorkerProvider with empty state setup for now (body will be filled in Plan 02)
- Placeholder comment: "// TODO: Full implementation in Plan 02"

**File: src/features/worker/WorkerDashboard.jsx (SKELETON)**
Create stub component:
```javascript
import React from 'react';
import { WorkerProvider } from './contexts/WorkerContext';

const WorkerDashboardContent = () => {
  return (
    <div className="min-h-screen bg-slate-50">
      {/* TODO: Full dashboard implementation in Plan 02 */}
      <p>Worker Dashboard - Loading...</p>
    </div>
  );
};

const WorkerDashboard = () => {
  return (
    <WorkerProvider>
      <WorkerDashboardContent />
    </WorkerProvider>
  );
};

export default WorkerDashboard;
```

3. Run: git add -A && git commit -m "feat(09-01): create worker feature directory structure"
</action>
  <verify>ls src/features/worker/ && ls src/features/worker/contexts/ && git log --oneline -1 | grep "feat(09-01)"</verify>
  <done>
    - src/features/worker/ directory exists
    - src/features/worker/contexts/WorkerContext.jsx exists (skeleton)
    - src/features/worker/WorkerDashboard.jsx exists (stub)
    - src/features/worker/index.js exists with exports
    - Git commit created
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify imports and export from app.jsx</name>
  <files>src/features/worker/index.js</files>
  <action>
1. Read src/app.jsx to find where WorkerDashboard is currently imported
2. Update the import to use the new feature export

Current in app.jsx (around lines 21-25):
```javascript
import TeamManager from './features/admin/TeamManager';
import ClientManager from './features/admin/ClientManager';
import ScheduleManager from './features/admin/ScheduleManager';
import ExpenseManager from './features/admin/ExpenseManager';
import ValidationPortal from './features/admin/ValidationPortal';
```

Add import:
```javascript
import { WorkerDashboard } from './features/worker';
```

3. Verify that WorkerDashboard is still rendered at line 3630-3633:
```javascript
{view === 'worker' && (
  <WorkerDashboard
    {...{ onLogout: handleLogout, onLogin: handleLogin, currentUser, setCurrentUser, currentMonth, setCurrentMonth, logs, clients, schedules, personalSchedules, mainFormData, setMainFormData, handleSaveEntry, saveToDb, handleDelete, approvals, handleApproveMonth, systemSettings, documents, appNotifications }}
  />
)}
```

4. Keep this render exactly as-is for now — Plan 02 will update to use new context hook
</action>
  <verify>grep -n "WorkerDashboard" src/app.jsx | head -5</verify>
  <done>
    - WorkerDashboard import added to app.jsx
    - WorkerDashboard still renders when view === 'worker'
  </done>
</task>

</tasks>

<verification>
- [ ] src/features/worker/ directory created with correct structure
- [ ] WorkerContext.jsx skeleton exports useWorker and WorkerProvider
- [ ] WorkerDashboard.jsx skeleton renders without errors
- [ ] src/app.jsx imports WorkerDashboard from new location
- [ ] Git commit created for skeleton
</verification>

<success_criteria>
- Worker feature directory exists at src/features/worker/
- WorkerContext.jsx exports useWorker and WorkerProvider
- WorkerDashboard.jsx renders as stub
- app.jsx imports from new location
- Commit created
</success_criteria>

<output>
After completion, create `.planning/phases/09-modularizacao-worker/09-01-SUMMARY.md`
</output>