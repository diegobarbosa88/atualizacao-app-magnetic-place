---
phase: 10
plan: 04
type: execute
wave: 2
depends_on: [10-03]
files_modified: []
autonomous: true
requirements: [CLEANUP-01]
user_setup: []

must_haves:
  truths:
    - "WorkerDashboard imports EntryForm, CompanyLogo, WorkerDocuments from common/"
    - "Duplicate component definitions removed from WorkerDashboard"
    - "Folder structure finalized with proper exports"
  artifacts:
    - path: "src/features/worker/WorkerDashboard.jsx"
      provides: "Updated worker dashboard using common components"
      updated: true
    - path: "src/components/common/index.js"
      provides: "Barrel export for common components"
  key_links:
    - from: "src/features/worker/WorkerDashboard.jsx"
      to: "src/components/common/EntryForm.jsx"
      via: "import"
      pattern: "import.*EntryForm.*from.*components/common"
---

<objective>
Update WorkerDashboard.jsx to import EntryForm, CompanyLogo, and WorkerDocuments from the new common/ location instead of using inline definitions. Finalize the folder structure with proper barrel exports.

Purpose: Remove all duplicate component definitions, complete the consolidation
Output: WorkerDashboard updated, common/index.js created
</objective>

<execution_context>
@C:/Users/diego/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/features/worker/WorkerDashboard.jsx (lines 1-843 - inline CompanyLogo, EntryForm, WorkerDocuments to remove)
@src/components/common/ (new common components from Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update WorkerDashboard imports to use common components</name>
  <files>src/features/worker/WorkerDashboard.jsx</files>
  <action>
UPDATE `src/features/worker/WorkerDashboard.jsx`:

1. **Remove inline definitions** (DELETE these from the file):
   - Delete `CompanyLogo` component definition (lines 23-33)
   - Delete `EntryForm` component definition (lines 35-140)
   - Delete `WorkerDocuments` component definition (lines 142-362)

2. **Add imports for common components**:
   ```jsx
   import CompanyLogo from '../../components/common/CompanyLogo';
   import EntryForm from '../../components/common/EntryForm';
   import WorkerDocuments from '../../components/common/WorkerDocuments';
   ```

3. **Update EntryForm usage**:
   - The WorkerDashboard EntryForm usage passes `systemSettings` as a prop
   - The common EntryForm should accept `systemSettings` as optional prop
   - WorkerDashboard passes: `systemSettings={systemSettings}` - this should work

4. **Update WorkerDocuments usage**:
   - WorkerDashboard passes `{ currentUser, documents, saveToDb }` - same as common signature
   - Should work without changes if the common component has the same signature

5. **Remove duplicate imports** that are now in common:
   - Remove `Calendar, Coffee, Save, X, Plus, Loader2, Sparkles` from WorkerDashboard if they were only used by EntryForm
   - Remove SignatureCanvas if only used by WorkerDocuments - but check if it's needed elsewhere

The WorkerDashboard already uses `useWorker()` hook which provides most data. The EntryForm in WorkerDashboard passes systemSettings from the useWorker context.
</action>
  <verify>
WorkerDashboard no longer has inline CompanyLogo, EntryForm, or WorkerDocuments definitions. `grep -c "const CompanyLogo\|const EntryForm\|const WorkerDocuments" src/features/worker/WorkerDashboard.jsx` returns 0. Common components are imported instead.
</verify>
  <done>WorkerDashboard updated to use common components with imports from '../../components/common/'</done>
</task>

<task type="auto">
  <name>Task 2: Create src/components/common/index.js barrel export</name>
  <files>src/components/common/index.js</files>
  <action>
Create `src/components/common/index.js` with barrel exports:

```js
export { default as CompanyLogo } from './CompanyLogo';
export { default as EntryForm } from './EntryForm';
export { default as WorkerDocuments } from './WorkerDocuments';
export { default as ClientTimesheetReport } from './ClientTimesheetReport';
```

This allows clean imports like:
```jsx
import { CompanyLogo, EntryForm } from '../components/common';
```
</action>
  <verify>
Barrel export file exists at src/components/common/index.js and exports all four common components. `grep -c "export.*CompanyLogo\|export.*EntryForm\|export.*WorkerDocuments\|export.*ClientTimesheetReport" src/components/common/index.js` returns 4.
</verify>
  <done>Barrel export created at src/components/common/index.js</done>
</task>

<task type="auto">
  <name>Task 3: Final cleanup - remove any remaining inline definitions in app.jsx</name>
  <files>src/app.jsx</files>
  <action>
FINAL VERIFICATION AND CLEANUP of app.jsx:

After Plan 03, app.jsx should already be clean. But verify:

1. **No inline component definitions remain** - grep for these patterns should return 0:
   - `const CompanyLogo = ` - should be in common/
   - `const EntryForm = ` - should be in common/
   - `const WorkerDocuments = ` - should be in common/
   - `const ClientTimesheetReport = ` - should be in common/
   - `const FinancialReportOverlay = ` - should be in features/admin/
   - `const DocumentsAdmin = ` - should be in features/admin/
   - `const NotificationsAdmin = ` - should be in features/admin/
   - `const LoginView = ` - should be in features/auth/
   - `const AdminDashboard = ` - should be in features/admin/

2. **Proper imports exist** at the top of app.jsx:
   - All extracted components imported from their new locations
   - No duplicate inline definitions

3. **Target line count**: Under 200 lines

4. **The App function** should be a clean router that:
   - Uses `useApp()` to get context values
   - Handles login/logout and notification management
   - Renders views based on `view` state
   - Keeps global modals that can appear across views

If any inline definitions remain from incomplete Plan 03, remove them now.
</action>
  <verify>
app.jsx has no inline component definitions. `grep -E "const (CompanyLogo|EntryForm|WorkerDocuments|ClientTimesheetReport|FinancialReportOverlay|DocumentsAdmin|NotificationsAdmin|LoginView|AdminDashboard) = " src/app.jsx` returns no matches. Line count under 200.
</verify>
  <done>Final cleanup complete - app.jsx is a pure router with no inline component definitions</done>
</task>

</tasks>

<verification>
Run the app and verify:
1. Login works - LoginView renders from features/auth/
2. Admin dashboard loads - AdminDashboard from features/admin/
3. Worker dashboard loads with EntryForm and WorkerDocuments from common/
4. Client portal loads - ClientTimesheetReport from common/
5. All tabs and features work as before
6. No console errors about missing imports
</verification>

<success_criteria>
1. WorkerDashboard imports from common/ instead of inline definitions
2. src/components/common/index.js has barrel exports for all common components
3. app.jsx is a pure router under 200 lines
4. All four views (login, admin, worker, client_portal) function correctly
5. No duplicate component definitions exist anywhere
6. Folder structure matches the research target:
   - src/components/common/ (shared components)
   - src/features/admin/ (admin-specific components)
   - src/features/auth/ (auth components)
   - src/features/worker/ (worker-specific, already existed)
</success_criteria>

<output>
After completion, create `.planning/phases/10-limpeza-roteamento/10-04-SUMMARY.md`
</output>
