---
phase: 18-reconciliacao-bancaria
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - api/reconciliacao/upload.js
autonomous: true
requirements:
  - BANK-01
  - BANK-02

must_haves:
  truths:
    - "Dependências csv-parser, ofx e formidable instaladas em package.json"
    - "API route /api/reconciliacao/upload.js criada com parseadores CSV e OFX"
    - "Parser CSV detecta colunas por cabeçalho e normaliza para Transacao[]"
    - "Parser OFX extrai transações e normaliza para Transacao[]"
    - "Ficheiros PDF são rejeitados com erro explícito"
    - "Output dos parsers: { data: 'YYYY-MM-DD', descricao: string, valor: number, tipo: 'credito'|'debito' }"
  artifacts:
    - path: "api/reconciliacao/upload.js"
      provides: "Vercel API Route com parsers CSV e OFX"
      contains: "export default async function handler"
    - path: "package.json"
      provides: "Dependências instaladas"
      contains: "csv-parse"
  key_links:
    - from: "api/reconciliacao/upload.js"
      to: "matching engine (Plan 03)"
      via: "função parseFile retorna Transacao[]"
      pattern: "parseFile|Transacao"
---

<objective>
Instalar as dependências de parsing e criar a Vercel API Route `/api/reconciliacao/upload.js` com os parsers CSV e OFX que normalizam transações bancárias para um formato uniforme.

Purpose: O matching engine (Plan 03) recebe sempre o mesmo `Transacao[]` independentemente do formato do ficheiro carregado.
Output: `api/reconciliacao/upload.js` funcional com parse de CSV e OFX, seguindo o padrão de `api/gmail/import-faturas.js`.
</objective>

<execution_context>
@C:\Users\diego\30 04 21h 19 app Magnetic\app Magnetic\.planning\phases\18-reconciliacao-bancaria\18-CONTEXT.md
</execution_context>

<context>
@C:\Users\diego\30 04 21h 19 app Magnetic\app Magnetic\.planning\ROADMAP.md
@C:\Users\diego\30 04 21h 19 app Magnetic\app Magnetic\.planning\phases\18-reconciliacao-bancaria\18-CONTEXT.md

<interfaces>
<!-- Padrão Vercel API Route existente (api/gmail/import-faturas.js): -->
export default async function handler(req, res) {
  if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' });
  // Usa supabase service role key para operações de escrita
  const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);
  // ... lógica ...
}

<!-- Tipo Transacao (output dos parsers): -->
// { data: string (YYYY-MM-DD), descricao: string, valor: number, tipo: 'credito'|'debito' }

<!-- Formato CSV português esperado: -->
// Colunas possíveis: Data, Data Valor, Valor, Descrição, Débito, Crédito
// Datas: DD-MM-YYYY ou DD/MM/YYYY → normalizar para YYYY-MM-DD
// Valor positivo = crédito, negativo = débito

<!-- Formato OFX: -->
// Tags relevantes: <DTPOSTED>, <TRNAMT>, <NAME> ou <MEMO>
// DTPOSTED: YYYYMMDD → normalizar para YYYY-MM-DD
// TRNAMT: positivo = crédito, negativo = débito
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar dependências csv-parse, ofx e formidable</name>
  <read_first>
    - package.json (verificar se já existem antes de instalar)
    - api/gmail/import-faturas.js (padrão de imports usado nas API routes)
  </read_first>
  <files>package.json</files>
  <action>
Executar no terminal (na raiz do projeto):

```bash
npm install csv-parse ofx formidable
```

Notas:
- Usar `csv-parse` (não `csv-parser`) — é a biblioteca recomendada para Node.js serverless (suporte a streams e promises).
- `ofx` para parsing de ficheiros OFX/QFX bancários portugueses.
- `formidable` para parsing de multipart/form-data nas Vercel API Routes (mesmo padrão que multer mas compatível com Edge).

Verificar após instalação que package.json contém as 3 dependências.
  </action>
  <verify>
    <automated>grep -c "csv-parse" "package.json"</automated>
  </verify>
  <acceptance_criteria>
    - grep "csv-parse" em package.json retorna match (com versão, e.g., "^5.5.6")
    - grep "\"ofx\"" em package.json retorna match
    - grep "formidable" em package.json retorna match
    - node_modules/csv-parse existe após npm install
  </acceptance_criteria>
  <done>3 dependências instaladas e presentes em package.json dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar API Route /api/reconciliacao/upload.js com parsers CSV e OFX</name>
  <read_first>
    - api/gmail/import-faturas.js (padrão exacto de handler, error handling, supabase client)
    - .planning/phases/18-reconciliacao-bancaria/18-CONTEXT.md §D-01, D-02, D-03, D-19, D-20
    - package.json (confirmar versões instaladas de csv-parse e ofx)
  </read_first>
  <files>api/reconciliacao/upload.js</files>
  <behavior>
    - Ficheiro PDF → res.status(400).json({ error: 'Formato não suportado. Apenas CSV e OFX são aceites.' })
    - CSV com colunas não reconhecidas → res.status(400).json({ error: 'Colunas não reconhecidas: ...', detected: [...] })
    - CSV válido com colunas Data + Valor → retorna array de Transacao[] normalizado
    - OFX válido → retorna array de Transacao[] normalizado
    - Data "15-03-2024" → normalizada para "2024-03-15"
    - Data "15/03/2024" → normalizada para "2024-03-15"
    - Valor positivo no CSV → tipo: 'credito'
    - Valor negativo no CSV → tipo: 'debito' com valor absoluto em .valor
    - Coluna separada Débito/Crédito → combinar: crédito positivo = credito, débito positivo = debito
  </behavior>
  <action>
Criar `api/reconciliacao/upload.js` com a seguinte estrutura:

```javascript
import formidable from 'formidable';
import fs from 'fs';
import { parse } from 'csv-parse/sync';
import ofxParser from 'ofx';
import { createClient } from '@supabase/supabase-js';

export const config = { api: { bodyParser: false } };

// Normaliza datas DD-MM-YYYY ou DD/MM/YYYY para YYYY-MM-DD
function normalizeDate(dateStr) {
  if (!dateStr) return null;
  // OFX format YYYYMMDD
  if (/^\d{8}$/.test(dateStr)) {
    return `${dateStr.slice(0,4)}-${dateStr.slice(4,6)}-${dateStr.slice(6,8)}`;
  }
  // Portuguese format DD-MM-YYYY or DD/MM/YYYY
  const match = dateStr.match(/^(\d{2})[-\/](\d{2})[-\/](\d{4})$/);
  if (match) return `${match[3]}-${match[2]}-${match[1]}`;
  return dateStr;
}

// Detecta colunas CSV por nome (case-insensitive)
function detectColumn(headers, candidates) {
  return candidates.find(c =>
    headers.some(h => h.toLowerCase().trim() === c.toLowerCase())
  );
}

function parseCsv(content) {
  const records = parse(content, { columns: true, skip_empty_lines: true, trim: true });
  if (!records.length) return [];

  const headers = Object.keys(records[0]);
  const dataCol = detectColumn(headers, ['data', 'data valor', 'date']);
  const valorCol = detectColumn(headers, ['valor', 'value', 'montante', 'amount']);
  const descricaoCol = detectColumn(headers, ['descrição', 'descricao', 'description', 'movimento', 'memo']);
  const debitoCol = detectColumn(headers, ['débito', 'debito', 'debit']);
  const creditoCol = detectColumn(headers, ['crédito', 'credito', 'credit']);

  // Se não reconhecer colunas mínimas
  if (!dataCol || (!valorCol && !debitoCol && !creditoCol)) {
    const err = new Error('UNRECOGNIZED_COLUMNS');
    err.detected = headers;
    throw err;
  }

  return records.map(row => {
    let valor, tipo;
    if (valorCol) {
      const raw = parseFloat(String(row[valorCol]).replace(',', '.').replace(/\s/g, ''));
      valor = Math.abs(raw);
      tipo = raw >= 0 ? 'credito' : 'debito';
    } else {
      const debito = parseFloat(String(row[debitoCol] || '0').replace(',', '.').replace(/\s/g, '')) || 0;
      const credito = parseFloat(String(row[creditoCol] || '0').replace(',', '.').replace(/\s/g, '')) || 0;
      if (credito > 0) { valor = credito; tipo = 'credito'; }
      else { valor = debito; tipo = 'debito'; }
    }
    return {
      data: normalizeDate(row[dataCol]),
      descricao: String(row[descricaoCol] || '').trim(),
      valor,
      tipo,
    };
  }).filter(t => t.valor > 0);
}

function parseOfx(content) {
  const parsed = ofxParser.parse(content);
  const stmtTrn = parsed?.OFX?.BANKMSGSRSV1?.STMTTRNRS?.STMTRS?.BANKTRANLIST?.STMTTRN;
  if (!stmtTrn) return [];
  const list = Array.isArray(stmtTrn) ? stmtTrn : [stmtTrn];
  return list.map(t => {
    const raw = parseFloat(t.TRNAMT);
    return {
      data: normalizeDate(String(t.DTPOSTED || '').slice(0, 8)),
      descricao: String(t.NAME || t.MEMO || '').trim(),
      valor: Math.abs(raw),
      tipo: raw >= 0 ? 'credito' : 'debito',
    };
  }).filter(t => t.valor > 0);
}

export default async function handler(req, res) {
  if (req.method !== 'POST') {
    return res.status(405).json({ error: 'Method not allowed' });
  }

  try {
    const form = formidable({ maxFileSize: 10 * 1024 * 1024 }); // 10MB limit
    const [, files] = await form.parse(req);
    const file = Array.isArray(files.file) ? files.file[0] : files.file;

    if (!file) return res.status(400).json({ error: 'Nenhum ficheiro recebido.' });

    const filename = file.originalFilename || file.newFilename || '';
    const ext = filename.split('.').pop().toLowerCase();

    // Rejeitar PDFs explicitamente
    if (ext === 'pdf' || file.mimetype === 'application/pdf') {
      return res.status(400).json({
        error: 'Formato não suportado. Apenas CSV e OFX são aceites. Ficheiros PDF não são processados.'
      });
    }

    if (!['csv', 'ofx', 'qfx'].includes(ext)) {
      return res.status(400).json({
        error: `Extensão .${ext} não suportada. Formatos aceites: .csv, .ofx, .qfx`
      });
    }

    const content = fs.readFileSync(file.filepath, 'utf-8');
    let transacoes;

    try {
      transacoes = ext === 'csv' ? parseCsv(content) : parseOfx(content);
    } catch (parseErr) {
      if (parseErr.message === 'UNRECOGNIZED_COLUMNS') {
        return res.status(400).json({
          error: 'Colunas do CSV não reconhecidas. Esperado: Data, Valor (ou Débito/Crédito), Descrição.',
          detected: parseErr.detected,
        });
      }
      throw parseErr;
    }

    // Matching engine será adicionado no Plan 03
    // Por agora retornar transações parseadas para validação
    return res.status(200).json({
      ok: true,
      filename,
      transaction_count: transacoes.length,
      transacoes,
      // matched, orphan_bank, orphan_system serão adicionados no Plan 03
    });

  } catch (err) {
    console.error('[reconciliacao/upload]', err);
    return res.status(500).json({ error: err.message || 'Erro interno no servidor.' });
  }
}
```

NOTA: O endpoint devolve `transacoes` para validação neste plan. O Plan 03 adiciona o matching engine e substitui o retorno por `{ matched, orphan_bank, orphan_system, run_id }`.
  </action>
  <verify>
    <automated>grep -c "parseCsv\|parseOfx\|normalizeDate" "api/reconciliacao/upload.js"</automated>
  </verify>
  <acceptance_criteria>
    - grep "export default async function handler" retorna match
    - grep "parseCsv" e "parseOfx" ambos presentes no ficheiro
    - grep "normalizeDate" retorna match com lógica YYYYMMDD e DD[-/]MM[-/]YYYY
    - grep "UNRECOGNIZED_COLUMNS" retorna match (erro explícito para CSV inválido)
    - grep "Ficheiros PDF não são processados" retorna match (rejeição explícita de PDF)
    - grep "export const config" retorna match (bodyParser: false para formidable)
    - grep "csv-parse/sync" retorna match (import correcto)
    - Ficheiro tem menos de 150 linhas (lean e focado)
  </acceptance_criteria>
  <done>API route criada com parsers CSV e OFX funcionais. Rejeição de PDF implementada. Output normalizado para Transacao[]. Plan 03 completa com matching engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/reconciliacao/upload | Ficheiro não confiável enviado pelo admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | File upload multipart | mitigate | formidable com maxFileSize: 10MB; validação explícita de extensão (.csv, .ofx, .qfx apenas) |
| T-18-05 | Denial of Service | Parsing de CSV grande | mitigate | Limite 10MB no formidable; csv-parse/sync é síncrono mas bounded pelo filesize |
| T-18-06 | Information Disclosure | Stack trace no response | mitigate | catch genérico retorna err.message sem stack trace; console.error para logs server-side |
</threat_model>

<verification>
Testar manualmente após deploy (ou com curl local se Vercel CLI disponível):

```bash
# Testar rejeição de PDF
curl -X POST http://localhost:3000/api/reconciliacao/upload \
  -F "file=@test.pdf" | grep "Ficheiros PDF"

# Testar CSV válido (criar ficheiro de teste com Data,Valor,Descricao)
echo "Data,Valor,Descrição" > /tmp/test.csv
echo "15-03-2024,150.00,Pagamento cliente" >> /tmp/test.csv
curl -X POST http://localhost:3000/api/reconciliacao/upload \
  -F "file=@/tmp/test.csv" | grep "transacoes"
```
</verification>

<success_criteria>
- npm install concluído sem erros; csv-parse, ofx, formidable em package.json
- api/reconciliacao/upload.js criado com parsers CSV e OFX
- PDF rejeitado com mensagem em PT
- CSV com colunas desconhecidas retorna erro com lista de colunas detectadas
- CSV válido retorna Transacao[] com data YYYY-MM-DD, descricao, valor numérico, tipo credito|debito
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/18-reconciliacao-bancaria/18-02-SUMMARY.md`
</output>
