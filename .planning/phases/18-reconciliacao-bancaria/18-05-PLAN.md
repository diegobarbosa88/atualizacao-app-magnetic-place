---
phase: 18-reconciliacao-bancaria
plan: 05
type: execute
wave: 4
depends_on:
  - "18-04"
files_modified:
  - src/features/admin/AdminDashboard.jsx
autonomous: false
requirements:
  - BANK-01
  - BANK-02
  - BANK-03
  - BANK-04
  - BANK-05

must_haves:
  truths:
    - "Tab 'Reconciliação' visível no menu de navegação do AdminDashboard, com ícone Landmark"
    - "Clicar em 'Reconciliação' renderiza ReconciliacaoAdmin"
    - "Upload de ficheiro CSV processa e mostra resultados em 3 sub-tabs"
    - "Botão Confirmar Pagamento actualiza status da fatura para PAGO no Supabase"
    - "Histórico colapsável lista runs anteriores com contagens"
  artifacts:
    - path: "src/features/admin/AdminDashboard.jsx"
      provides: "Tab reconciliacao no nav array e renderização condicional"
      contains: "reconciliacao.*ReconciliacaoAdmin\|ReconciliacaoAdmin.*reconciliacao"
  key_links:
    - from: "AdminDashboard nav array"
      to: "ReconciliacaoAdmin component"
      via: "activeTab === 'reconciliacao'"
      pattern: "activeTab.*reconciliacao\|reconciliacao.*activeTab"
---

<objective>
Ligar `ReconciliacaoAdmin.jsx` ao `AdminDashboard.jsx`: adicionar a tab "Reconciliação" ao nav array e ao label renderer, importar o componente, e adicionar o bloco de renderização condicional.

Purpose: Tornar o módulo de reconciliação acessível ao admin via navegação existente.
Output: Tab "Reconciliação" com ícone Landmark no menu do AdminDashboard, funcional end-to-end.
</objective>

<execution_context>
@C:\Users\diego\30 04 21h 19 app Magnetic\app Magnetic\.planning\phases\18-reconciliacao-bancaria\18-CONTEXT.md
</execution_context>

<context>
@C:\Users\diego\30 04 21h 19 app Magnetic\app Magnetic\.planning\ROADMAP.md
@C:\Users\diego\30 04 21h 19 app Magnetic\app Magnetic\.planning\phases\18-reconciliacao-bancaria\18-CONTEXT.md
@C:\Users\diego\30 04 21h 19 app Magnetic\app Magnetic\.planning\phases\18-reconciliacao-bancaria\18-04-SUMMARY.md

<interfaces>
<!-- Nav array actual (linha 396 de AdminDashboard.jsx): -->
['overview', 'team', 'clients', 'portal_validacao', 'schedules', 'documentos', 'costs', 'settings']

<!-- Label renderer actual (linha 400): -->
t === 'overview' ? 'Geral'
  : t === 'team' ? 'Equipa'
  : t === 'clients' ? 'Clientes'
  : t === 'portal_validacao' ? (...)
  : t === 'schedules' ? 'Horários'
  : t === 'costs' ? 'Custos'
  : t === 'documentos' ? 'Documentos'
  : <Settings size={14} />   // ← fallback para 'settings'

<!-- Padrão de renderização condicional (linhas 1002-1013): -->
{!auditWorkerId && activeTab === 'documentos' && (
  <DocumentsAdmin ... />
)}
{!auditWorkerId && activeTab === 'settings' && (
  <div ...>...</div>
)}

<!-- Import de ReconciliacaoAdmin: -->
import ReconciliacaoAdmin from './ReconciliacaoAdmin';

<!-- Ícone a importar: Landmark (já disponível no lucide-react) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar tab Reconciliação e importar ReconciliacaoAdmin no AdminDashboard</name>
  <read_first>
    - src/features/admin/AdminDashboard.jsx (ler linhas 1-35 para imports, linhas 393-403 para nav array, linhas 1000-1020 para blocos de renderização)
    - src/features/admin/ReconciliacaoAdmin.jsx (confirmar que export default existe)
  </read_first>
  <files>src/features/admin/AdminDashboard.jsx</files>
  <action>
Fazer 3 alterações precisas em AdminDashboard.jsx:

**Alteração 1 — Adicionar import do componente** (após a linha com `import NotificationsAdmin`):
```javascript
import ReconciliacaoAdmin from './ReconciliacaoAdmin';
```

**Alteração 2 — Adicionar 'reconciliacao' ao nav array** (linha ~396):
Alterar:
```javascript
{['overview', 'team', 'clients', 'portal_validacao', 'schedules', 'documentos', 'costs', 'settings'].map(t => (
```
Para:
```javascript
{['overview', 'team', 'clients', 'portal_validacao', 'schedules', 'documentos', 'costs', 'reconciliacao', 'settings'].map(t => (
```

**Alteração 3 — Adicionar label 'Reconciliação' no renderer** (linha ~400):
Alterar o trecho do label renderer. Localizar a parte:
```javascript
: t === 'costs' ? 'Custos' : t === 'documentos' ? 'Documentos' : <Settings size={14} />
```
Substituir por:
```javascript
: t === 'costs' ? 'Custos' : t === 'documentos' ? 'Documentos' : t === 'reconciliacao' ? 'Reconciliação' : <Settings size={14} />
```

**Alteração 4 — Adicionar bloco de renderização condicional** (após o bloco `activeTab === 'documentos'`, antes do bloco `activeTab === 'notificacoes'`):
```javascript
{!auditWorkerId && activeTab === 'reconciliacao' && (
  <ReconciliacaoAdmin />
)}
```

**Importante:** O ícone `Landmark` já está disponível via lucide-react — NÃO adicionar ao bloco de imports de ícones porque o ícone não é usado no AdminDashboard directamente (está dentro de ReconciliacaoAdmin.jsx).
  </action>
  <verify>
    <automated>grep -c "reconciliacao\|ReconciliacaoAdmin" "src/features/admin/AdminDashboard.jsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep "import ReconciliacaoAdmin from './ReconciliacaoAdmin'" retorna match exacto
    - grep "'reconciliacao'" retorna pelo menos 3 ocorrências (no array, no label renderer, no bloco condicional)
    - grep "activeTab === 'reconciliacao'" retorna match
    - grep "Reconciliação" retorna match no label renderer (com acento)
    - grep "ReconciliacaoAdmin />" retorna match (self-closing, sem props adicionais)
    - O array de tabs contém 'reconciliacao' ANTES de 'settings' (ordem correcta)
    - npm run build não gera erros de compilação
  </acceptance_criteria>
  <done>AdminDashboard tem tab Reconciliação funcional. ReconciliacaoAdmin renderizado quando activeTab === 'reconciliacao'.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação end-to-end do módulo de reconciliação</name>
  <what-built>
    - Tab "Reconciliação" adicionada ao AdminDashboard
    - ReconciliacaoAdmin.jsx com upload drag & drop, matching engine via API, sub-tabs de resultados, confirmação de pagamento, formulário manual e histórico
    - Migrações SQL aplicadas no Supabase (faturas + reconciliation_runs)
    - Dependências csv-parse, ofx, formidable instaladas
  </what-built>
  <how-to-verify>
    1. Executar `npm run dev` e abrir o admin dashboard
    2. Verificar que a tab "Reconciliação" aparece no nav entre "Custos" e as Definições (ícone de engrenagem)
    3. Clicar em "Reconciliação" — deve aparecer o componente com zona de upload
    4. Testar inserção manual de fatura:
       - Clicar "Inserir Fatura Manual"
       - Preencher: Tipo=fatura, Valor=150, Data=hoje, Entidade=TESTE SA
       - Clicar Guardar — deve confirmar sem erro
    5. Criar um ficheiro CSV de teste com conteúdo:
       ```
       Data,Valor,Descrição
       19-05-2026,150.00,Pagamento TESTE SA referente maio
       25-05-2026,500.00,Transferência sem correspondência
       ```
    6. Arrastar o ficheiro CSV para a zona de upload — deve aparecer o nome do ficheiro
    7. Clicar "Processar Extrato" — deve aparecer loading e depois os 3 sub-tabs com contagens
    8. Verificar sub-tab "Reconciliados": deve mostrar a transação de €150 com botão "Confirmar Pagamento"
    9. Verificar sub-tab "Órfãos Banco": deve mostrar a transação de €500
    10. Verificar sub-tab "Órfãos Sistema": deve estar vazio (a fatura de €150 foi reconciliada)
    11. Clicar "Confirmar Pagamento" na linha de €150 — botão muda para "Pago" com checkmark
    12. No Supabase Dashboard → Table Editor → faturas: verificar que a fatura de TESTE SA tem status = 'PAGO'
    13. Expandir a secção "Histórico" — deve mostrar o run recém-criado com contagens correctas
    14. Tentar carregar um ficheiro PDF — deve mostrar mensagem de erro em PT sem processar
  </how-to-verify>
  <resume-signal>Escreve "aprovado" se todos os 14 passos passaram, ou descreve quais falharam.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AdminDashboard → ReconciliacaoAdmin | Componente sem props de segurança — usa supabase via useApp() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-12 | Elevation of Privilege | Tab reconciliacao sem auth guard | accept | AdminDashboard só é renderizado para utilizadores admin autenticados — sem risco adicional |
</threat_model>

<verification>
```bash
# Compilação sem erros:
npm run build 2>&1 | tail -20

# Verificar 4 alterações aplicadas:
grep -n "ReconciliacaoAdmin\|reconciliacao\|Reconciliação" src/features/admin/AdminDashboard.jsx
# Deve mostrar: import (linha ~25), array nav (linha ~396), label renderer (linha ~400), bloco condicional (linha ~1007)
```
</verification>

<success_criteria>
- `npm run build` sem erros
- Tab "Reconciliação" visível no AdminDashboard entre "Custos" e Definições
- Upload de CSV processa e mostra resultados correctos em 3 sub-tabs
- Botão "Confirmar Pagamento" actualiza status no Supabase (verificado no Table Editor)
- Histórico lista runs anteriores com data, ficheiro e contagens
- PDF rejeitado com mensagem em PT
- Todas as 5 fases de reconciliação cobertas: BANK-01 a BANK-05
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/18-reconciliacao-bancaria/18-05-SUMMARY.md`
</output>
