---
phase: 18-reconciliacao-bancaria
plan: 03
type: execute
wave: 2
depends_on:
  - "18-01"
  - "18-02"
files_modified:
  - api/reconciliacao/upload.js
autonomous: true
requirements:
  - BANK-03
  - BANK-04
  - BANK-05

must_haves:
  truths:
    - "Matching engine aplica Regra 1 (valor exato) contra faturas com status PENDENTE"
    - "Matching engine aplica Regra 2 (substring na descrição) quando há múltiplos candidatos de mesmo valor"
    - "Resultados classificados em 3 grupos: matched, orphan_bank, orphan_system"
    - "Cada run é gravado na tabela reconciliation_runs com os JSONs completos"
    - "API devolve estrutura completa com run_id, matched[], orphan_bank[], orphan_system[]"
  artifacts:
    - path: "api/reconciliacao/upload.js"
      provides: "API route completa com matching engine integrado"
      contains: "runMatchingEngine\|orphan_bank\|orphan_system"
  key_links:
    - from: "matching engine"
      to: "tabela faturas (Supabase)"
      via: "supabase.from('faturas').select().eq('status','PENDENTE')"
      pattern: "status.*PENDENTE"
    - from: "matching engine"
      to: "tabela reconciliation_runs"
      via: "supabase.from('reconciliation_runs').insert(...)"
      pattern: "reconciliation_runs.*insert"
---

<objective>
Implementar o matching engine na API Route `/api/reconciliacao/upload.js`, que cruza as transações parseadas com as faturas pendentes do Supabase e grava o resultado em `reconciliation_runs`.

Purpose: Núcleo da reconciliação — transforma um extrato bancário em três grupos accionáveis (reconciliados, órfãos banco, órfãos sistema).
Output: API route completa que devolve `{ run_id, matched[], orphan_bank[], orphan_system[], counts }`.
</objective>

<execution_context>
@C:\Users\diego\30 04 21h 19 app Magnetic\app Magnetic\.planning\phases\18-reconciliacao-bancaria\18-CONTEXT.md
</execution_context>

<context>
@C:\Users\diego\30 04 21h 19 app Magnetic\app Magnetic\.planning\ROADMAP.md
@C:\Users\diego\30 04 21h 19 app Magnetic\app Magnetic\.planning\phases\18-reconciliacao-bancaria\18-CONTEXT.md
@C:\Users\diego\30 04 21h 19 app Magnetic\app Magnetic\.planning\phases\18-reconciliacao-bancaria\18-02-SUMMARY.md

<interfaces>
<!-- Tipo Transacao (output dos parsers do Plan 02): -->
// { data: string, descricao: string, valor: number, tipo: 'credito'|'debito' }

<!-- Schema tabela faturas (após migração Plan 01): -->
// id UUID, tipo TEXT, valor NUMERIC, data_documento DATE,
// descricao TEXT, entidade TEXT, status TEXT ('PENDENTE'|'PAGO'),
// fonte TEXT ('gmail'|'toc'|'manual'), ficheiro_url TEXT

<!-- Schema tabela reconciliation_runs (Plan 01): -->
// id UUID, created_at, filename TEXT, transaction_count INT,
// matched_count INT, orphan_bank_count INT, orphan_system_count INT,
// transactions_json JSONB, results_json JSONB

<!-- Regras do matching engine (D-04, D-05, D-06, D-07): -->
// Regra 1: fatura.valor === transacao.valor (numérico exato)
// Regra 2: fatura.entidade.toLowerCase() está contida em transacao.descricao.toLowerCase()
// Transacao states: matched | ambiguous | orphan_bank
// Fatura states: matched | orphan_system | pending

<!-- Supabase client pattern (api/gmail/import-faturas.js): -->
const supabase = createClient(
  process.env.SUPABASE_URL,
  process.env.SUPABASE_SERVICE_ROLE_KEY
);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar matching engine e completar API Route</name>
  <read_first>
    - api/reconciliacao/upload.js (estado actual do Plan 02 — parsers já implementados)
    - .planning/phases/18-reconciliacao-bancaria/18-CONTEXT.md §D-04, D-05, D-06, D-07, D-08, D-11
    - api/gmail/import-faturas.js (padrão de supabase client e error handling)
  </read_first>
  <files>api/reconciliacao/upload.js</files>
  <behavior>
    - Regra 1: transação com valor 150.00 → fatura com valor 150.00 e status PENDENTE → matched (1:1)
    - Regra 1 + 2: 2 faturas com valor 150.00 + descrição "EMPRESA XPTO" → fatura com entidade "EMPRESA XPTO" na descrição → matched, outra → orphan_system
    - Regra 2 (ambiguous): 2 faturas com mesmo valor, nenhuma entidade na descrição → transação fica ambiguous
    - Transação sem candidatos → orphan_bank
    - Faturas sem correspondência → orphan_system
    - Run gravado em reconciliation_runs com transactions_json e results_json
    - response.json inclui run_id (UUID do registo criado)
  </behavior>
  <action>
Substituir o bloco a partir de "// Matching engine será adicionado no Plan 03" até ao final do handler com a implementação completa.

Adicionar a função `runMatchingEngine` ANTES do `export default`:

```javascript
// ─── Matching Engine ──────────────────────────────────────────────────────────

/**
 * @param {Array} transacoes - Transacao[] dos parsers
 * @param {Array} faturas    - Faturas com status PENDENTE do Supabase
 * @returns {{ matched, orphan_bank, orphan_system }}
 *
 * Regra 1: valor numérico exato (fatura.valor === transacao.valor)
 * Regra 2: fatura.entidade substring na transacao.descricao (case-insensitive)
 */
function runMatchingEngine(transacoes, faturas) {
  const matched = [];
  const orphan_bank = [];
  const usedFaturaIds = new Set();

  for (const transacao of transacoes) {
    // Regra 1: candidatos por valor exato
    const candidatos = faturas.filter(f =>
      !usedFaturaIds.has(f.id) &&
      Number(f.valor) === transacao.valor
    );

    if (candidatos.length === 0) {
      orphan_bank.push({ transacao, reason: 'no_match' });
      continue;
    }

    if (candidatos.length === 1) {
      matched.push({ transacao, fatura: candidatos[0], rule: 'exact_value' });
      usedFaturaIds.add(candidatos[0].id);
      continue;
    }

    // Regra 2: desambiguação por descrição
    const descLower = (transacao.descricao || '').toLowerCase();
    const porDescricao = candidatos.filter(f =>
      f.entidade && descLower.includes(f.entidade.toLowerCase())
    );

    if (porDescricao.length === 1) {
      matched.push({ transacao, fatura: porDescricao[0], rule: 'description_match' });
      usedFaturaIds.add(porDescricao[0].id);
      continue;
    }

    // Ambiguous — múltiplos candidatos, Regra 2 não resolveu
    orphan_bank.push({
      transacao,
      reason: 'ambiguous',
      candidates: candidatos.map(f => ({ id: f.id, entidade: f.entidade, valor: f.valor })),
    });
  }

  // Faturas sem correspondência
  const orphan_system = faturas
    .filter(f => !usedFaturaIds.has(f.id))
    .map(f => ({ fatura: f, reason: 'no_transaction' }));

  return { matched, orphan_bank, orphan_system };
}
```

Substituir o final do handler (após `transacoes = ...`) com:

```javascript
    // Buscar faturas pendentes do Supabase
    const supabase = createClient(
      process.env.SUPABASE_URL,
      process.env.SUPABASE_SERVICE_ROLE_KEY
    );

    const { data: faturas, error: fatError } = await supabase
      .from('faturas')
      .select('id, tipo, valor, data_documento, descricao, entidade, status, fonte')
      .eq('status', 'PENDENTE');

    if (fatError) throw new Error(`Supabase query failed: ${fatError.message}`);

    const { matched, orphan_bank, orphan_system } = runMatchingEngine(
      transacoes,
      faturas || []
    );

    // Gravar run em reconciliation_runs
    const { data: run, error: runError } = await supabase
      .from('reconciliation_runs')
      .insert({
        filename,
        transaction_count: transacoes.length,
        matched_count: matched.length,
        orphan_bank_count: orphan_bank.length,
        orphan_system_count: orphan_system.length,
        transactions_json: transacoes,
        results_json: { matched, orphan_bank, orphan_system },
      })
      .select('id')
      .single();

    if (runError) throw new Error(`Failed to save run: ${runError.message}`);

    return res.status(200).json({
      ok: true,
      run_id: run.id,
      filename,
      transaction_count: transacoes.length,
      matched_count: matched.length,
      orphan_bank_count: orphan_bank.length,
      orphan_system_count: orphan_system.length,
      matched,
      orphan_bank,
      orphan_system,
    });
```
  </action>
  <verify>
    <automated>grep -c "runMatchingEngine\|orphan_bank\|orphan_system\|reconciliation_runs" "api/reconciliacao/upload.js"</automated>
  </verify>
  <acceptance_criteria>
    - grep "runMatchingEngine" retorna match (função definida e chamada)
    - grep "\.eq('status', 'PENDENTE')" retorna match (query Supabase correcta)
    - grep "orphan_bank" retorna pelo menos 3 ocorrências (definição + retorno + response)
    - grep "orphan_system" retorna pelo menos 3 ocorrências
    - grep "reconciliation_runs.*insert\|insert.*reconciliation_runs" retorna match
    - grep "run_id: run.id" retorna match (run_id na resposta)
    - grep "'ambiguous'" retorna match (estado de transação ambígua tratado)
    - grep "description_match\|exact_value" retorna match (regras identificadas no resultado)
    - grep "SUPABASE_SERVICE_ROLE_KEY" retorna match (service role para escrita)
    - Função runMatchingEngine não importa nenhuma dependência externa — apenas lógica pura
  </acceptance_criteria>
  <done>Matching engine implementado com Regra 1 (valor exato) e Regra 2 (descrição substring). Run gravado em reconciliation_runs. API devolve { run_id, matched[], orphan_bank[], orphan_system[], counts }.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matching engine → Supabase faturas | Leitura de dados financeiros via service role |
| API route → reconciliation_runs | Escrita de dados bancários (JSONs de transações) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | Information Disclosure | transactions_json em reconciliation_runs | accept | Dados bancários do próprio admin; RLS limita acesso; dados não saem do Supabase |
| T-18-08 | Tampering | Matching engine manipulado via ficheiro CSV crafted | mitigate | Matching apenas lê faturas existentes; não altera BD automaticamente (D-08 non-destructive) — confirmação manual requerida no frontend |
| T-18-09 | Repudiation | Sem auditoria de quem fez o upload | accept | MVP de um único admin; auditoria via created_at em reconciliation_runs é suficiente |
</threat_model>

<verification>
```bash
# Validar estrutura do ficheiro após edição:
grep -n "runMatchingEngine\|eq.*PENDENTE\|reconciliation_runs\|run_id" api/reconciliacao/upload.js

# Deve mostrar:
# - linha com definição de runMatchingEngine
# - linha com .eq('status', 'PENDENTE')
# - linha com .from('reconciliation_runs').insert(
# - linha com run_id: run.id
```
</verification>

<success_criteria>
- API route devolve `{ run_id, matched[], orphan_bank[], orphan_system[] }` após processamento
- Matching Regra 1 (valor exato) funcional
- Matching Regra 2 (substring entidade na descrição) funcional para desambiguação
- Run gravado em reconciliation_runs com todos os campos do D-11
- Faturas sem match ficam em orphan_system
- Transações sem match ficam em orphan_bank
- Transações ambíguas marcadas com reason: 'ambiguous' e lista de candidatos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/18-reconciliacao-bancaria/18-03-SUMMARY.md`
</output>
