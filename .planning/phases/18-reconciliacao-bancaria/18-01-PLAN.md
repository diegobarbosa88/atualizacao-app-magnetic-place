---
phase: 18-reconciliacao-bancaria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260519_reconciliacao_faturas_columns.sql
  - supabase/migrations/20260519_reconciliation_runs.sql
autonomous: false
requirements:
  - BANK-01
  - BANK-04
  - BANK-05

must_haves:
  truths:
    - "A tabela `faturas` tem as colunas de reconciliação: tipo, valor, data_documento, descricao, entidade, status, fonte, ficheiro_url"
    - "A tabela `reconciliation_runs` existe com campos id, created_at, filename, transaction_count, matched_count, orphan_bank_count, orphan_system_count, transactions_json, results_json"
    - "RLS ativo em ambas as tabelas com política de leitura pelo admin"
    - "As migrações aplicadas no Supabase (visíveis no Table Editor)"
  artifacts:
    - path: "supabase/migrations/20260519_reconciliacao_faturas_columns.sql"
      provides: "ALTER TABLE faturas com novos campos de reconciliação"
      contains: "ALTER TABLE faturas ADD COLUMN IF NOT EXISTS tipo"
    - path: "supabase/migrations/20260519_reconciliation_runs.sql"
      provides: "CREATE TABLE reconciliation_runs"
      contains: "CREATE TABLE IF NOT EXISTS reconciliation_runs"
  key_links:
    - from: "tabela faturas"
      to: "matching engine (Plan 03)"
      via: "campo status = 'PENDENTE'"
      pattern: "status.*PENDENTE"
    - from: "tabela reconciliation_runs"
      to: "API route (Plan 03)"
      via: "INSERT após cada run"
      pattern: "reconciliation_runs"
---

<objective>
Criar as migrações SQL para estender a tabela `faturas` com campos de reconciliação e criar a tabela `reconciliation_runs` para histórico.

Purpose: O matching engine (Plan 03) precisa das colunas `tipo`, `valor`, `data_documento`, `descricao`, `entidade`, `status`, `fonte` na tabela `faturas`. O histórico de runs requer a tabela `reconciliation_runs`.
Output: 2 ficheiros SQL de migração prontos para aplicar no Supabase Dashboard.
</objective>

<execution_context>
@C:\Users\diego\30 04 21h 19 app Magnetic\app Magnetic\.planning\phases\18-reconciliacao-bancaria\18-CONTEXT.md
</execution_context>

<context>
@C:\Users\diego\30 04 21h 19 app Magnetic\app Magnetic\.planning\ROADMAP.md
@C:\Users\diego\30 04 21h 19 app Magnetic\app Magnetic\.planning\phases\18-reconciliacao-bancaria\18-CONTEXT.md

<interfaces>
<!-- Tabela faturas existente (de 20260518_faturas_table.sql + 20260518_faturas_dados.sql): -->
-- Colunas actuais:
--   id UUID PRIMARY KEY DEFAULT gen_random_uuid()
--   gmail_message_id TEXT NOT NULL
--   filename TEXT NOT NULL
--   storage_path TEXT NOT NULL
--   url TEXT NOT NULL
--   mime_type TEXT
--   tamanho INTEGER
--   importado_em TIMESTAMPTZ DEFAULT NOW()
--   processado BOOLEAN DEFAULT FALSE
--   dados JSONB

-- Campos a adicionar (D-13):
--   tipo TEXT CHECK (tipo IN ('fatura','recibo'))
--   valor NUMERIC
--   data_documento DATE
--   descricao TEXT
--   entidade TEXT   -- nome trabalhador/cliente/NIF
--   status TEXT NOT NULL DEFAULT 'PENDENTE' CHECK (status IN ('PENDENTE','PAGO'))
--   fonte TEXT NOT NULL DEFAULT 'gmail' CHECK (fonte IN ('gmail','toc','manual'))
--   ficheiro_url TEXT

-- Nova tabela reconciliation_runs (D-11):
--   id UUID PRIMARY KEY DEFAULT gen_random_uuid()
--   created_at TIMESTAMPTZ DEFAULT NOW()
--   filename TEXT NOT NULL
--   transaction_count INTEGER DEFAULT 0
--   matched_count INTEGER DEFAULT 0
--   orphan_bank_count INTEGER DEFAULT 0
--   orphan_system_count INTEGER DEFAULT 0
--   transactions_json JSONB
--   results_json JSONB
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migração — Estender tabela `faturas` com campos de reconciliação</name>
  <read_first>
    - supabase/migrations/20260518_faturas_table.sql (schema actual da tabela faturas)
    - supabase/migrations/20260518_faturas_dados.sql (coluna dados JSONB já adicionada)
    - .planning/phases/18-reconciliacao-bancaria/18-CONTEXT.md §D-13 (definição dos campos)
  </read_first>
  <files>supabase/migrations/20260519_reconciliacao_faturas_columns.sql</files>
  <action>
Criar o ficheiro SQL com ADD COLUMN IF NOT EXISTS para cada campo novo da tabela `faturas`.

Conteúdo exacto do ficheiro:

```sql
-- Phase 18: Add reconciliation columns to faturas table
-- Extends the Phase 17 Gmail import table with financial matching fields

ALTER TABLE faturas ADD COLUMN IF NOT EXISTS tipo TEXT
  CHECK (tipo IN ('fatura', 'recibo'));

ALTER TABLE faturas ADD COLUMN IF NOT EXISTS valor NUMERIC;

ALTER TABLE faturas ADD COLUMN IF NOT EXISTS data_documento DATE;

ALTER TABLE faturas ADD COLUMN IF NOT EXISTS descricao TEXT;

ALTER TABLE faturas ADD COLUMN IF NOT EXISTS entidade TEXT;

ALTER TABLE faturas ADD COLUMN IF NOT EXISTS status TEXT NOT NULL DEFAULT 'PENDENTE'
  CHECK (status IN ('PENDENTE', 'PAGO'));

ALTER TABLE faturas ADD COLUMN IF NOT EXISTS fonte TEXT NOT NULL DEFAULT 'gmail'
  CHECK (fonte IN ('gmail', 'toc', 'manual'));

ALTER TABLE faturas ADD COLUMN IF NOT EXISTS ficheiro_url TEXT;

-- Index for fast matching queries (matching engine filters by status = 'PENDENTE')
CREATE INDEX IF NOT EXISTS faturas_status_idx ON faturas (status);
CREATE INDEX IF NOT EXISTS faturas_valor_idx ON faturas (valor);
```
  </action>
  <verify>
    <automated>grep -c "ALTER TABLE faturas ADD COLUMN IF NOT EXISTS status" "supabase/migrations/20260519_reconciliacao_faturas_columns.sql"</automated>
  </verify>
  <acceptance_criteria>
    - grep "ADD COLUMN IF NOT EXISTS tipo" retorna match
    - grep "ADD COLUMN IF NOT EXISTS valor" retorna match
    - grep "ADD COLUMN IF NOT EXISTS status" retorna match com CHECK (status IN ('PENDENTE', 'PAGO'))
    - grep "ADD COLUMN IF NOT EXISTS fonte" retorna match com CHECK (fonte IN ('gmail', 'toc', 'manual'))
    - grep "ADD COLUMN IF NOT EXISTS entidade" retorna match
    - grep "CREATE INDEX IF NOT EXISTS faturas_status_idx" retorna match
    - Ficheiro tem 8 ALTER TABLE statements
  </acceptance_criteria>
  <done>Ficheiro SQL criado com todos os campos de D-13, indexes de performance incluídos.</done>
</task>

<task type="auto">
  <name>Task 2: Migração — Criar tabela `reconciliation_runs`</name>
  <read_first>
    - .planning/phases/18-reconciliacao-bancaria/18-CONTEXT.md §D-11 (definição dos campos)
    - supabase/migrations/20260518_faturas_table.sql (padrão RLS usado na tabela faturas)
  </read_first>
  <files>supabase/migrations/20260519_reconciliation_runs.sql</files>
  <action>
Criar o ficheiro SQL com CREATE TABLE e RLS para `reconciliation_runs`.

Conteúdo exacto do ficheiro:

```sql
-- Phase 18: Reconciliation runs history table
-- Each bank statement import creates one row here

CREATE TABLE IF NOT EXISTS reconciliation_runs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  filename TEXT NOT NULL,
  transaction_count INTEGER NOT NULL DEFAULT 0,
  matched_count INTEGER NOT NULL DEFAULT 0,
  orphan_bank_count INTEGER NOT NULL DEFAULT 0,
  orphan_system_count INTEGER NOT NULL DEFAULT 0,
  transactions_json JSONB,
  results_json JSONB
);

-- RLS: only authenticated admin users can read
ALTER TABLE reconciliation_runs ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Admin can read reconciliation_runs"
  ON reconciliation_runs FOR SELECT
  USING (true);

CREATE POLICY "Service role can insert reconciliation_runs"
  ON reconciliation_runs FOR INSERT
  WITH CHECK (true);

-- Index for history list sorted by date
CREATE INDEX IF NOT EXISTS reconciliation_runs_created_at_idx
  ON reconciliation_runs (created_at DESC);
```
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS reconciliation_runs" "supabase/migrations/20260519_reconciliation_runs.sql"</automated>
  </verify>
  <acceptance_criteria>
    - grep "CREATE TABLE IF NOT EXISTS reconciliation_runs" retorna match
    - grep "transactions_json JSONB" retorna match
    - grep "results_json JSONB" retorna match
    - grep "ENABLE ROW LEVEL SECURITY" retorna match
    - grep "reconciliation_runs_created_at_idx" retorna match
    - Todos os 9 campos do D-11 presentes: id, created_at, filename, transaction_count, matched_count, orphan_bank_count, orphan_system_count, transactions_json, results_json
  </acceptance_criteria>
  <done>Ficheiro SQL criado com schema completo e RLS configurado.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>[BLOCKING] Task 3: Aplicar migrações no Supabase Dashboard</name>
  <read_first>
    - supabase/migrations/20260519_reconciliacao_faturas_columns.sql (conteúdo a executar)
    - supabase/migrations/20260519_reconciliation_runs.sql (conteúdo a executar)
  </read_first>
  <what-built>2 ficheiros SQL de migração criados nas tasks anteriores.</what-built>
  <how-to-verify>
    1. Abrir o Supabase Dashboard → SQL Editor
    2. Copiar e executar o conteúdo de `supabase/migrations/20260519_reconciliacao_faturas_columns.sql`
    3. Verificar que retorna "Success. No rows returned."
    4. Copiar e executar o conteúdo de `supabase/migrations/20260519_reconciliation_runs.sql`
    5. Verificar que retorna "Success. No rows returned."
    6. No Table Editor, verificar que a tabela `faturas` tem as colunas: tipo, valor, data_documento, descricao, entidade, status (default 'PENDENTE'), fonte (default 'gmail'), ficheiro_url
    7. No Table Editor, verificar que a tabela `reconciliation_runs` existe com os campos corretos
  </how-to-verify>
  <resume-signal>Escreve "aplicado" após confirmar que ambas as tabelas estão corretas no Supabase Dashboard.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → Supabase | Operações de leitura/escrita diretas no frontend via anon key |
| API Route → Supabase | Operações de escrita via service role key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | faturas.status field | mitigate | RLS: apenas admin pode atualizar status via UI; API route usa service role apenas para INSERT em reconciliation_runs |
| T-18-02 | Information Disclosure | reconciliation_runs (dados bancários em JSON) | mitigate | RLS ativo; USING (true) suficiente para admin único — rever se multi-tenant |
| T-18-03 | Elevation of Privilege | CHECK constraint em status/fonte | mitigate | DB-level CHECK impede valores inválidos mesmo se frontend enviar dados maliciosos |
</threat_model>

<verification>
Após aplicação das migrações:

```sql
-- Executar no SQL Editor para validar:
SELECT column_name, data_type, column_default, is_nullable
FROM information_schema.columns
WHERE table_name = 'faturas'
  AND column_name IN ('tipo','valor','data_documento','descricao','entidade','status','fonte','ficheiro_url')
ORDER BY column_name;
-- Deve retornar 8 linhas

SELECT table_name FROM information_schema.tables
WHERE table_name = 'reconciliation_runs';
-- Deve retornar 1 linha
```
</verification>

<success_criteria>
- Tabela `faturas` tem os 8 novos campos com constraints CHECK corretos
- Tabela `reconciliation_runs` existe com os 9 campos do D-11
- RLS ativo em ambas as tabelas
- Indexes de performance criados (faturas_status_idx, faturas_valor_idx, reconciliation_runs_created_at_idx)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/18-reconciliacao-bancaria/18-01-SUMMARY.md`
</output>
