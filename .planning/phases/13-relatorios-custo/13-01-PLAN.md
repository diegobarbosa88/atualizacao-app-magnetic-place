---
phase: 13-relatorios-custo
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/admin/AdminDashboard.jsx
  - src/features/admin/CostReports.jsx
  - src/utils/formatUtils.js
autonomous: true
requirements:
  - COST-01
  - COST-02
  - COST-03
must_haves:
  truths:
    - Admin pode ver custo total por trabalhador para um período
    - Admin pode ver custo total por cliente para um período
    - Relatório mostra horas × valor/hora em euros
  artifacts:
    - path: src/features/admin/CostReports.jsx
      provides: Componente de relatórios de custo
      min_lines: 80
    - path: src/features/admin/AdminDashboard.jsx
      provides: Integração do relatório no dashboard admin
      exports: CostReports import
  key_links:
    - from: src/features/admin/CostReports.jsx
      to: src/context/AppContext.jsx
      via: useApp() hook - dados de logs e approvals
      pattern: logs.*filter|approvals.*filter
---

<objective>
Criar relatórios de custo por trabalhador e cliente no Admin Dashboard.
</objective>

<execution_context>
@C:/Users/diego/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-relatorios-custo/13-CONTEXT.md
@.planning/phases/13-relatorios-custo/13-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Existing Code Patterns

From src/features/admin/ClientManager.jsx:
```javascript
// Estrutura de tabela usada no projeto
<div className="overflow-x-auto">
  <table className="min-w-full divide-y divide-slate-200">
    <thead className="bg-slate-50">
      <tr>
        <th className="px-6 py-3 text-left text-xs font-medium text-slate-500 uppercase">Nome</th>
        <th className="px-6 py-3 text-left text-xs font-medium text-slate-500 uppercase">Horas</th>
        <th className="px-6 py-3 text-left text-xs font-medium text-slate-500 uppercase">Custo</th>
      </tr>
    </thead>
    <tbody className="bg-white divide-y divide-slate-200">
```

From src/features/admin/ScheduleManager.jsx:
```javascript
// Filtro de período usado no projeto
<select
  value={selectedMonth}
  onChange={(e) => setSelectedMonth(e.target.value)}
  className="rounded-md border-slate-300"
>
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CostReports component with calculation logic</name>
  <files>src/features/admin/CostReports.jsx</files>
  <read_first>
- src/features/admin/ClientManager.jsx (table structure pattern)
- src/features/admin/ScheduleManager.jsx (month/year filter pattern)
- src/context/AppContext.jsx (useApp hook, logs/approvals data structure)
- src/utils/formatUtils.js (existing formatters)
  </read_first>
  <action>
Criar o componente CostReports.jsx com:

1. **Imports:**
   - useApp do context
   - useState para selectedMonth, selectedYear, activeTab (workers/clients)

2. **State:**
   - selectedMonth (current month)
   - selectedYear (current year)
   - activeTab ('workers' | 'clients')

3. **Worker Cost Calculation (useMemo):**
   - Filter logs por month/year do selectedMonth/selectedYear
   - Group by workerId, SUM(hours)
   - Multiplicar por workers.valorHora
   - Retornar array: { id, name, totalHours, cost }

4. **Client Cost Calculation (useMemo):**
   - Filter approvals por month/year
   - Group by clientId, SUM(approvedHours)
   - Multiplicar por clients.valorHora
   - Retornar array: { id, name, totalHours, cost }

5. **Render:**
   - Tabs para Workers / Clients (estilo lucide-react icons)
   - Selector de mês/ano (igual a ScheduleManager)
   - Tabela com colunas: Nome, Total Horas, Custo (€)
   - Totais no rodapé da tabela

6. **Styling:**
   - Usar mesma estrutura de tabelas do ClientManager
   - Headers: indigo-600 background, uppercase text
   - Valores em euros com 2 decimais
   - Totais em bold

7. **Export default:** CostReports componente
  </action>
  <acceptance_criteria>
- File src/features/admin/CostReports.jsx exists
- Contains "export default CostReports"
- Contains "useApp()" hook call
- Contains useMemo for worker cost calculation
- Contains useMemo for client cost calculation
- Has month/year selector (select element)
- Has tab navigation (workers/clients)
- Table renders with columns: Nome, Total Horas, Custo
  </acceptance_criteria>
  <verify>
grep -c "export default" src/features/admin/CostReports.jsx
grep -c "useApp" src/features/admin/CostReports.jsx
  </verify>
  <done>CostReports.jsx created with worker and client cost calculations, tab navigation, and month/year filters</done>
</task>

<task type="auto">
  <name>Task 2: Integrate CostReports into AdminDashboard</name>
  <files>src/features/admin/AdminDashboard.jsx</files>
  <read_first>
- src/features/admin/AdminDashboard.jsx (existing navigation structure)
- src/features/admin/index.js (existing exports pattern)
  </read_first>
  <action>
Em AdminDashboard.jsx:

1. **Import CostReports:**
   ```javascript
   import CostReports from './CostReports';
   ```

2. **Add navigation option:**
   - No menu/navegação do admin, adicionar "Relatórios de Custo"
   - Ou como secção no dashboard principal
   - Usar mesma estrutura de navegação existente

3. **Render CostReports:**
   - Adicionar secção ou view para o componente
   - Manter consistência com o estilo existente do admin

4. **Ou alternativo (se AdminDashboard for muito complexo):**
   - Criar entrada no menu existente com navigate para cost-reports
   - O componente já tem toda a lógica inline
  </action>
  <acceptance_criteria>
- AdminDashboard.jsx imports CostReports
- CostReports appears in the admin navigation/menu
- Or CostReports renders in a section of the dashboard
  </acceptance_criteria>
  <verify>
grep -c "CostReports" src/features/admin/AdminDashboard.jsx
  </verify>
  <done>CostReports accessible from AdminDashboard menu</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → CostReports | Dados do AppContext são confiáveis (já validados pelo contexto) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | CostReports | accept | Dados são do próprio admin, sem risco |
| T-13-02 | Denial of Service | useMemo calculations | mitigate | Limitar resultados com pagination se >100 items |

</threat_model>

<verification>
- [ ] CostReports.jsx renders without errors
- [ ] Worker cost calculation is correct (horas × valorHora)
- [ ] Client cost calculation is correct (aprovadas × valorHora)
- [ ] Month/year filter works
- [ ] Tab navigation works between workers/clients
- [ ] Accessible from AdminDashboard
</verification>

<success_criteria>
Admin can view cost reports for workers and clients filtered by month/year with totals in euros.
</success_criteria>

<output>
After completion, create `.planning/phases/13-relatorios-custo/13-01-SUMMARY.md`
</output>