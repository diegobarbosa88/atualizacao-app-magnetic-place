---
phase: 11-gestao-trabalhadores
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/admin/contexts/ScheduleContext.jsx
  - src/features/admin/ScheduleManager.jsx
  - src/features/admin/contexts/TeamContext.jsx
autonomous: true
requirements:
  - WORKER-02
must_haves:
  truths:
    - Admin pode definir período de validade ao atribuir horário
    - Atribuições passadas não são substituídas por novas
    - Histórico de atribuições保留了 (preserved)
  artifacts:
    - path: src/features/admin/contexts/ScheduleContext.jsx
      provides: Schedule assignment with temporal validity
  key_links:
    - from: ScheduleManager.jsx
      to: worker_assigned_schedules table
      via: saveToDb for schedule assignments
---

<objective>
Adicionar datas de início e fim de validade à atribuição de horários. Quando um novo horário é atribuído a um trabalhador, deve criar um novo registo com datas de validade em vez de substituir atribuições anteriores.
</objective>

<execution_context>
@C:/Users/diego/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/diego/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-gestao-trabalhadores/11-CONTEXT.md
@.planning/ROADMAP.md

From src/features/admin/ScheduleManager.jsx (lines 97-110):
```javascript
// Current checkbox assignment - needs date validity
<input type="checkbox" 
  checked={workerForm.assignedSchedules?.includes(s.id)} 
  onChange={() => {
    const current = workerForm.assignedSchedules || [];
    const updated = current.includes(s.id) 
      ? current.filter(id => id !== s.id)
      : [...current, s.id];
    setWorkerForm({ ...workerForm, assignedSchedules: updated });
  }}
/>
```

From src/features/admin/contexts/ScheduleContext.jsx (lines 28-38):
```javascript
// Current assignment logic - needs temporal validation
const handleAssignSchedule = async (workerId, scheduleId) => {
  const w = workers.find(w => w.id === workerId);
  const currentAssigned = w.assignedSchedules || [];
  if (!currentAssigned.includes(scheduleId)) {
    const nextAssigned = [...currentAssigned, scheduleId];
    await saveToDb('workers', w.id, { ...w, assignedSchedules: nextAssigned });
  }
};
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create worker_schedule_assignments table schema</name>
  <files>src/features/admin/contexts/ScheduleContext.jsx</files>
  <read_first>
    - src/features/admin/ScheduleManager.jsx (assignment UI)
    - src/features/admin/contexts/ScheduleContext.jsx (assignment logic)
  </read_first>
  <action>
Criar nova tabela para atribuições com validade temporal. Esta tabela armazena cada atribuição com suas datas:

```
Table: worker_schedule_assignments
Columns:
- id: uuid (primary key)
- worker_id: uuid (foreign key to workers)
- schedule_id: uuid (foreign key to schedules)
- data_inicio: date (quando começa a vigencia)
- data_fim: date (quando termina a vigencia, nullable = ativo)
- created_at: timestamp
```

A atribuição atual (assignedSchedules no worker) será transição gradual para esta nova estrutura.

Para manter compatibilidade, primeiro implementar a lógica de atribuição com datas na UI.
  </action>
  <verify>
    <automated>grep -c "worker_schedule_assignments\|data_inicio\|data_fim" src/features/admin/contexts/ScheduleContext.jsx</automated>
  </verify>
  <done>Assignment schema supports temporal validity</done>
</task>

<task type="auto">
  <name>Task 2: Update ScheduleManager UI with date inputs</name>
  <files>src/features/admin/ScheduleManager.jsx</files>
  <read_first>
    - src/features/admin/ScheduleManager.jsx (assignment checkboxes section)
  </read_first>
  <action>
Na secção de atribuição de horários (ScheduleManager.jsx), quando o admin clica para atribuir um horário a um trabalhador, mostrar modal ou campos de data:

```javascript
// Dentro do componente de atribuição de horário
const [assignmentDates, setAssignmentDates] = useState({
  workerId: null, scheduleId: null, dataInicio: '', dataFim: ''
});

// Na UI de atribuição (onde está o checkbox):
{/* Adicionar campos de data ao lado do checkbox quando em modo de atribuição */}
<div className="flex items-center gap-2">
  <input type="checkbox" 
    checked={workerForm.assignedSchedules?.includes(s.id)}
    onChange={() => openDatePicker(worker.id, s.id)}
  />
  {assignmentDates.workerId === worker.id && assignmentDates.scheduleId === s.id && (
    <div className="flex gap-2">
      <input type="date" 
        value={assignmentDates.dataInicio}
        onChange={e => setAssignmentDates({...assignmentDates, dataInicio: e.target.value})}
        placeholder="Início"
      />
      <input type="date"
        value={assignmentDates.dataFim}
        onChange={e => setAssignmentDates({...assignmentDates, dataFim: e.target.value})}
        placeholder="Fim"
      />
    </div>
  )}
</div>
```
  </action>
  <verify>
    <automated>grep -c "dataInicio.*dataFim" src/features/admin/ScheduleManager.jsx</automated>
  </verify>
  <done>UI allows setting validity dates for schedule assignments</done>
</task>

<task type="auto">
  <name>Task 3: Save assignments with dates (preserve history)</name>
  <files>src/features/admin/contexts/ScheduleContext.jsx</files>
  <read_first>
    - src/features/admin/contexts/ScheduleContext.jsx (handleAssignSchedule)
  </read_first>
  <action>
Modificar handleAssignSchedule para criar registos com datas em vez de apenas adicionar ao array:

```javascript
// D-04, D-05: Atribuição com datas de validade preserva histórico
const handleAssignScheduleWithDates = async (workerId, scheduleId, dataInicio, dataFim) => {
  // Criar nova atribuição com datas (não substituir)
  const assignment = {
    worker_id: workerId,
    schedule_id: scheduleId,
    data_inicio: dataInicio,
    data_fim: dataFim || null  // null = ativo indefinidamente
  };
  
  // Salvar na nova tabela
  await saveToDb('worker_schedule_assignments', null, assignment);
  
  // Também manter compatibilidade com assignedSchedules array para queries simples
  const w = workers.find(w => w.id === workerId);
  const currentAssigned = w.assignedSchedules || [];
  if (!currentAssigned.includes(scheduleId)) {
    const nextAssigned = [...currentAssigned, scheduleId];
    await saveToDb('workers', w.id, { ...w, assignedSchedules: nextAssigned });
  }
};
```
  </action>
  <verify>
    <automated>grep -c "worker_schedule_assignments" src/features/admin/contexts/ScheduleContext.jsx</automated>
  </verify>
  <done>Schedule assignments saved with date validity and history preserved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted date inputs for assignment validity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Injection | Assignment date fields | mitigate | Validate date format; dataInicio <= dataFim |
| T-11-04 | Repudiation | Assignment history | mitigate | Log created_at timestamp; preserve all assignments |
</threat_model>

<verification>
- [ ] New table/structure for temporal assignments exists
- [ ] UI allows setting dataInicio and dataFim for assignments
- [ ] New assignments are added without removing old ones
- [ ] Historical assignments are queryable
</verification>

<success_criteria>
1. Admin can set validity dates when assigning schedule to worker
2. Previous assignments are preserved (not overwritten)
3. Multiple assignments for same worker+schedule are tracked with different dates

After completion, create `.planning/phases/11-gestao-trabalhadores/11-02-SUMMARY.md`
</success_criteria>

<output>
Create .planning/phases/11-gestao-trabalhadores/11-02-SUMMARY.md after execution
</output>