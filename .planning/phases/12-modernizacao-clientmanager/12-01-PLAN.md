---
phase: 12-modernizacao-clientmanager
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/admin/ClientManager.jsx
autonomous: true
requirements:
  - UI-MOD-01
user_setup: []

must_haves:
  truths:
    - "Formulário de cliente usa layout 2 colunas (8/4) como TeamManager"
    - "Secções visuais com headers coloridos (bg-slate-50/50, bg-emerald-50/30)"
    - "Labels uppercase com ícones lucide-react"
    - "Classes CSS consistentes: rounded-[2.5rem], rounded-[2rem], shadow-xl, border-slate-100"
    - "Todas as funcionalidades originais preservadas (CRUD, histórico valor hora, sorting, views)"
  artifacts:
    - path: "src/features/admin/ClientManager.jsx"
      provides: "Interface de gestão de clientes com design system atualizado"
      min_lines: 350
  key_links:
    - from: "src/features/admin/ClientManager.jsx"
      to: "src/features/admin/TeamManager.jsx"
      via: "design system reference"
      pattern: "replicate.*TeamManager"
---

<objective>
Modernizar a UI do ClientManager.jsx aplicando o design system de alta fidelidade usado no TeamManager.jsx e ScheduleManager.jsx.

Purpose: Criar consistência visual no admin dashboard, com layout 2 colunas, secções visuais, e styling unificado.

Output: Ficheiro ClientManager.jsx refatorado com novo design system, mantendo toda a lógica existente.
</objective>

<execution_context>
@C:/Users/diego/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/diego/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

# Design System Reference
@src/features/admin/TeamManager.jsx (linhas 117-290 - form layout 2 colunas)
@src/features/admin/ClientManager.jsx (estado atual - precisa de modernização)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatorar formulário de cliente para layout 2 colunas</name>
  <files>src/features/admin/ClientManager.jsx</files>
  <read_first>
    - src/features/admin/TeamManager.jsx (linhas 117-290: estrutura do formulário 2 colunas)
    - src/features/admin/ClientManager.jsx (estado atual, preservar lógica)
  </read_first>
  <action>
## Transformar formulário de cliente (isAddingInTab)

### Estrutura atual (a remover):
- grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6
- Labels simples sem ícones
- bg-white p-10 rounded-[3rem]

### Nova estrutura ( TeamManager style):
```
<div className="grid grid-cols-1 lg:grid-cols-12 gap-8">
  {/* COLUNA ESQUERDA (8 colunas) */}
  <div className="lg:col-span-8 space-y-8">
    
    {/* DADOS DO CLIENTE */}
    <div className="bg-slate-50/50 p-6 rounded-[2rem] border border-slate-100 space-y-6">
      <div className="flex items-center gap-3 mb-2">
        <div className="p-2 bg-indigo-100 text-indigo-600 rounded-xl"><Briefcase size={18} /></div>
        <h4 className="font-black text-slate-700 text-lg uppercase tracking-tight">Dados do Cliente</h4>
      </div>
      
      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
        {/* Campos: name, nif, morada, email */}
      </div>
    </div>

    {/* DADOS FINANCEIROS */}
    <div className="bg-emerald-50/30 p-6 rounded-[2rem] border border-emerald-100 space-y-6">
      <div className="flex items-center gap-3 mb-2">
        <div className="p-2 bg-emerald-100 text-emerald-600 rounded-xl"><Euro size={18} /></div>
        <h4 className="font-black text-emerald-800 text-lg uppercase tracking-tight">Dados Financeiros</h4>
      </div>
      
      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
        {/* Campos: valorHora, dataAlteracao */}
      </div>
    </div>
  </div>

  {/* COLUNA DIREITA (4 colunas) */}
  <div className="lg:col-span-4 space-y-6">
    {/* Secção: Horários / Ações */}
    <div className="bg-slate-50 p-6 rounded-[2rem] border border-slate-100">
      {/* Lista de trabalhadores asociados (opcional - se aplicável) */}
    </div>

    {/* Botões de ação */}
    <div className="pt-2">
      <button onClick={handleSaveClient}>Gravar Cliente</button>
    </div>
  </div>
</div>
```

### Labels com ícones (exemplo TeamManager):
```jsx
<label className="text-[10px] font-black text-slate-400 uppercase tracking-wider ml-1 flex items-center gap-1">
  <Briefcase size={10} /> Nome da Empresa
</label>
<input className="... focus:border-indigo-400 focus:ring-4 focus:ring-indigo-50 ..." />
```

### Header do formulário:
```jsx
<div className="flex justify-between items-center mb-8 border-b border-slate-100 pb-6">
  <h3 className="text-2xl font-black text-slate-800 flex items-center gap-3">
    <Briefcase className="text-indigo-600" size={28} />
    {clientForm.id ? 'Editar Cliente' : 'Novo Cliente'}
  </h3>
  <button onClick={() => setIsAddingInTab(false)} className="... bg-slate-50 hover:bg-slate-100 ...">
    <X size={16} /> Fechar Form
  </button>
</div>
```

### Inputs com focus states:
```jsx
className="w-full bg-white border border-slate-200 rounded-xl p-4 text-sm font-bold outline-none shadow-sm focus:border-indigo-400 focus:ring-4 focus:ring-indigo-50 transition-all"
```

Manter TODA a lógica existente:
- handleSaveClient
- handleDeleteClient
- clientForm, setClientForm
- sortedClients
- loadClientValorHoraHistory
- showClientHistory, clientValorHoraHistory
  </action>
  <verify>
    <automated>grep -c "lg:col-span-8" src/features/admin/ClientManager.jsx</automated>
  </verify>
  <done>
    Formulário usa layout 2 colunas (8/4), secções com headers coloridos (indigo/emerald), labels uppercase com ícones
  </done>
</task>

<task type="auto">
  <name>Task 2: Modernizar visualização em lista e grelha</name>
  <files>src/features/admin/ClientManager.jsx</files>
  <read_first>
    - src/features/admin/TeamManager.jsx (linhas 293-399: table e grid views)
  </read_first>
  <action>
## Modernizar vistas de lista e grelha

### Table View (已有, melhorar styling):

Manter estrutura atual mas adicionar:
- Hover state consistente: hover:border-indigo-200 transition-all
- Focus rings nos botões de ação

### Grid View (card styling):

Atual:
```jsx
<div className="bg-white p-6 rounded-[2rem] border border-slate-100 shadow-sm hover:border-indigo-200 transition-all">
```

Mudar para TeamManager style:
```jsx
<div className="bg-white p-6 rounded-[2.5rem] border border-slate-100 shadow-md hover:shadow-lg hover:border-indigo-200 hover:-translate-y-1 transition-all duration-300">
```

Adicionar gradient subtle no header do card:
```jsx
<div className="flex justify-between items-start mb-4">
  <div className="p-3 bg-gradient-to-br from-indigo-50 to-indigo-100 rounded-2xl text-indigo-600">
    <Briefcase size={24} />
  </div>
</div>
```
  </action>
  <verify>
    <automated>grep -c "rounded-\[2\.5rem\]" src/features/admin/ClientManager.jsx</automated>
  </verify>
  <done>
    Cards com styling consistente (rounded-[2.5rem], shadow-md, hover effects)
  </done>
</task>

<task type="auto">
  <name>Task 3: Verificar preservação de funcionalidades</name>
  <files>src/features/admin/ClientManager.jsx</files>
  <action>
## Verificar que todas as funcionalidades originais existem:

### Funcionalidades a preservar:
1. ✅ CRUD completo (handleSaveClient, handleDeleteClient)
2. ✅ Ordenação (clientsSort, setClientsSort) 
3. ✅ Vista em grelha/lista (clientsView, setClientsView)
4. ✅ Histórico de valor hora (loadClientValorHoraHistory, showClientHistory, clientValorHoraHistory)
5. ✅ Modal de histórico (linhas 147-174)
6. ✅ Validação de formulário
7. ✅ Integração com ClientContext (useClient)

### Importações a manter:
- Briefcase, LayoutGrid, List, Edit2, Trash2, MapPin, Euro (lucide-react)
- useApp, useClient, ClientProvider

### Estados a preservar:
- isAddingInTab, setIsAddingInTab
- clientsView, setClientsView  
- clientsSort, setClientsSort
- clientForm, setClientForm
- showClientHistory, setShowClientHistory
- clientValorHoraHistory, setClientValorHoraHistory
  </action>
  <verify>
    <automated>grep -c "handleSaveClient\|handleDeleteClient\|loadClientValorHoraHistory" src/features/admin/ClientManager.jsx</automated>
  </verify>
  <done>
    Todas as funcionalidades originais preservadas e funcionando
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→form | Dados de cliente atravessam aqui |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | ClientManager.jsx | accept | Apenas UI refactoring, lógica inalterada |
| T-12-02 | Info Disclosure | Histórico valor hora | accept | Dados já existentes no frontend |
</threat_model>

<verification>
- [ ] grep "lg:col-span-8" retorna contagem > 0
- [ ] grep "rounded-\[2\.5rem\]" retorna contagem > 0  
- [ ] grep "handleSaveClient\|handleDeleteClient" retorna contagem >= 2
- [ ] grep "loadClientValorHoraHistory" retorna contagem >= 1
</verification>

<success_criteria>
1. Formulário com layout 2 colunas (8/4) como TeamManager
2. Secções visuais: DADOS DO CLIENTE (indigo), DADOS FINANCEIROS (emerald)
3. Labels uppercase com ícones lucide-react
4. Classes CSS: rounded-[2.5rem], rounded-[2rem], shadow-xl, border-slate-100
5. Todas as funcionalidades preservadas: CRUD, histórico, sorting, views
</success_criteria>

<output>
After completion, create .planning/phases/12-modernizacao-clientmanager/12-01-SUMMARY.md
</output>