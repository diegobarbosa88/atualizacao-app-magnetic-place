---
phase: 11-gestao-trabalhadores
plan: 04
type: execute
wave: 1
depends_on:
  - 11-01
files_modified:
  - src/features/worker/WorkerDashboard.jsx
  - src/features/admin/TeamManager.jsx
  - src/context/AppContext.jsx
autonomous: true
requirements:
  - WORKER-05
must_haves:
  truths:
    - Trabalhador só vê notificações de meses >= dataInicio
    - Trabalhador só pode preencher horas de meses >= dataInicio
    - Se dataFim existe, acesso bloqueado após essa data
    - Admin tem filtro para mostrar/ocultar inativos na lista
    - Cálculos e relatórios filtrados por dataInicio do worker
---

<objective>
Implementar acesso condicional por data de início/fim do trabalhador. Se um trabalhador entrou em Abril, não deve ver notificações de Março, nem ter acesso para preencher horas de Março, nem aparecer em cálculos de meses anteriores.
</objective>

<execution_context>
@C:/Users/diego/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/diego/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-gestao-trabalhadores/11-CONTEXT.md
@.planning/ROADMAP.md

From src/features/worker/WorkerDashboard.jsx (pending approvals section):
```javascript
// Current: shows all pending approvals regardless of dataInicio
{pendingApprovals.map((pending, idx) => {
  const isViewingThisMonth = pending.monthStr === currentMonthStr;
```

From src/features/admin/TeamManager.jsx (worker list):
```javascript
// Current: shows all workers without filtering by status
{displayWorkers?.map(w => (
```

From src/context/AppContext.jsx (cost calculation):
```javascript
// Current: calculates costs for all logs regardless of worker dataInicio
if (worker) expectedCosts += l.hours * (Number(worker.valorHora) || 0);
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter pendingApprovals by dataInicio</name>
  <files>src/features/worker/WorkerDashboard.jsx</files>
  <read_first>
    - src/features/worker/WorkerDashboard.jsx (pendingApprovals section ~line 128)
    - src/features/admin/contexts/TeamContext.jsx (currentUser dataInicio access)
  </read_first>
  <action>
Filtrar as notificações de aprovação para só mostrar meses >= dataInicio do trabalhador:

```javascript
// Obter data de início do trabalhador atual
const workerDataInicio = currentUser?.dataInicio;
// Se não tem data de início, mostrar todas as notificações
const workerStartDate = workerDataInicio
  ? new Date(workerDataInicio)
  : null;

// Filtrar pendingApprovals - só meses >= dataInicio
const filteredPendingApprovals = pendingApprovals.filter(pending => {
  if (!workerStartDate) return true; // Sem data de início = sem filtro

  const pendingMonth = new Date(pending.date);
  // Criar data do primeiro dia do mês pendente
  const pendingMonthStart = new Date(pendingMonth.getFullYear(), pendingMonth.getMonth(), 1);
  return pendingMonthStart >= workerStartDate;
});
```

Substituir `{pendingApprovals.map` por `{filteredPendingApprovals.map`.
  </action>
  <verify>
    <automated>grep -c "filteredPendingApprovals\|workerStartDate" src/features/worker/WorkerDashboard.jsx</automated>
  </verify>
  <done>Pending approvals filtered by dataInicio</done>
</task>

<task type="auto">
  <name>Task 2: Block hour filling for months before dataInicio</name>
  <files>src/features/worker/WorkerDashboard.jsx</files>
  <read_first>
    - src/features/worker/WorkerDashboard.jsx (EntryForm section ~line 270)
    - src/features/worker/WorkerDashboard.jsx (day logs rendering)
  </read_first>
  <action>
Bloquear o formulário de registo de horas para meses anteriores à data de início do trabalhador:

```javascript
// Helper para verificar se o mês é acessível
const isMonthAccessible = (date) => {
  if (!currentUser?.dataInicio) return true; // Sem data = acessível

  const monthStart = new Date(date.getFullYear(), date.getMonth(), 1);
  const workerStart = new Date(currentUser.dataInicio);
  return monthStart >= workerStart;
};

// Helper para verificar se dataFim bloqueou o acesso
const isAccessBlockedByFim = () => {
  if (!currentUser?.dataFim) return false;

  const workerEnd = new Date(currentUser.dataFim);
  const today = new Date();
  return today > workerEnd;
};

// No EntryForm / formulário principal:
const canFillHours = isMonthAccessible(currentMonth) && !isAccessBlockedByFim();

{!canFillHours && (
  <div className="bg-amber-50 border border-amber-200 rounded-2xl p-4 text-center">
    <p className="text-sm font-bold text-amber-600">
      Só podes registar horas a partir de {new Date(currentUser.dataInicio).toLocaleDateString('pt-PT', { month: 'long', year: 'numeric' })}
    </p>
  </div>
)}

{canFillHours && (
  <EntryForm ... />
)}
```

Também bloquear dias individuais na lista:
```javascript
// Para cada dia na lista de dias
const dayDate = new Date(ds);
// Se o dia é antes da data de início, desabilitar botões de adicionar
const isDayBeforeStart = workerStartDate && dayDate < workerStartDate;

<button
  disabled={isDayBeforeStart}
  className={`... ${isDayBeforeStart ? 'opacity-50 cursor-not-allowed' : ''}`}
  ...
>
```
  </action>
  <verify>
    <automated>grep -c "isMonthAccessible\|canFillHours\|isDayBeforeStart" src/features/worker/WorkerDashboard.jsx</automated>
  </verify>
  <done>Hour filling blocked for months before dataInicio</done>
</task>

<task type="auto">
  <name>Task 3: Block access after dataFim</name>
  <files>src/features/worker/WorkerDashboard.jsx</files>
  <read_first>
    - src/features/worker/WorkerDashboard.jsx (main render section)
  </read_first>
  <action>
Se dataFim existe e já passou, bloquear todo o acesso ao WorkerDashboard:

```javascript
// No topo do componente WorkerDashboardContent, antes do return do JSX:
const isAccessBlockedByFim = () => {
  if (!currentUser?.dataFim) return false;
  const workerEnd = new Date(currentUser.dataFim);
  const today = new Date();
  today.setHours(0, 0, 0, 0); // Normalizar para meia-noite
  return today > workerEnd;
};

if (isAccessBlockedByFim()) {
  return (
    <div className="min-h-screen bg-slate-50 flex items-center justify-center p-4">
      <div className="bg-white rounded-3xl p-8 text-center max-w-md shadow-xl">
        <div className="text-6xl mb-4">🔒</div>
        <h2 className="text-2xl font-black text-slate-800 mb-2">Acesso Bloqueado</h2>
        <p className="text-sm text-slate-500 mb-4">
          O teu acesso foi desactivado em {new Date(currentUser.dataFim).toLocaleDateString('pt-PT')}.
        </p>
        <p className="text-xs text-slate-400">
          Contacta o administrador se precisas de reativar o acesso.
        </p>
      </div>
    </div>
  );
}
```

Isto é prioritário sobre qualquer outra lógica — se dataFim já passou, bloqueia tudo.
  </action>
  <verify>
    <automated>grep -c "isAccessBlockedByFim" src/features/worker/WorkerDashboard.jsx</automated>
  </verify>
  <done>Access blocked after dataFim date</done>
</task>

<task type="auto">
  <name>Task 4: Admin filter to show/hide inactive workers</name>
  <files>src/features/admin/TeamManager.jsx</files>
  <read_first>
    - src/features/admin/TeamManager.jsx (worker list rendering)
  </read_first>
  <action>
Adicionar filtro toggle para mostrar/ocultar trabalhadores inativos na lista de workers:

```javascript
// No estado do formulário ou no topo do componente:
const [showInactive, setShowInactive] = useState(false);

// Filtrar workers para display
const displayWorkers = workers.filter(w => {
  if (showInactive) return true; // Mostrar todos
  return w.status !== 'inativo'; // Só ativos
});

// No topo da lista de workers, adicionar toggle:
<div className="flex items-center justify-between mb-4">
  <h2 className="text-xl font-black">Trabalhadores</h2>
  <label className="flex items-center gap-2 text-xs font-bold text-slate-500 cursor-pointer">
    <input
      type="checkbox"
      checked={showInactive}
      onChange={e => setShowInactive(e.target.checked)}
      className="rounded border-slate-300"
    />
    Mostrar inativos ({workers.filter(w => w.status === 'inativo').length})
  </label>
</div>
```

O contador entre parênteses mostra quantos inativos existem (mesmo quando ocultos), para o admin saber que existem.
  </action>
  <verify>
    <automated>grep -c "showInactive\|Mostrar inativos" src/features/admin/TeamManager.jsx</automated>
  </verify>
  <done>Admin can toggle inactive workers visibility</done>
</task>

<task type="auto">
  <name>Task 5: Filter reports/calculations by worker dataInicio</name>
  <files>src/context/AppContext.jsx</files>
  <read_first>
    - src/context/AppContext.jsx (cost/revenue calculations in logs rendering)
    - src/features/admin/ReportsPanel.jsx (se existir - cálculos de relatórios)
  </read_first>
  <action>
Filtrar os registos de horas por data de início do trabalhador nos cálculos de relatórios e custos:

```javascript
// Função helper para verificar se um log pertence a um worker ativo
const isWorkerActiveInMonth = (worker, logDate) => {
  if (!worker) return false;

  const logMonth = new Date(logDate);
  logMonth.setDate(1); // Primeiro dia do mês

  // Verificar data de início
  if (worker.dataInicio) {
    const startDate = new Date(worker.dataInicio);
    if (logMonth < startDate) return false;
  }

  // Verificar data de fim
  if (worker.dataFim) {
    const endDate = new Date(worker.dataFim);
    if (logMonth > endDate) return false;
  }

  return true;
};

// Nos cálculos de expectedCosts e expectedRevenue:
// Substituir:
// if (worker) expectedCosts += l.hours * (Number(worker.valorHora) || 0);
// Por:
if (worker && isWorkerActiveInMonth(worker, l.date)) {
  expectedCosts += l.hours * (Number(worker.valorHora) || 0);
}
```

Isto garante que só horas de meses em que o trabalhador estava ativo são incluídas nos cálculos.
  </action>
  <verify>
    <automated>grep -c "isWorkerActiveInMonth" src/context/AppContext.jsx</automated>
  </verify>
  <done>Reports filtered by worker active period</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | dataInicio/dataFim from worker record |
| admin→API | showInactive filter on worker list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|----------------|
| T-11-07 | Tampering | Manipular dataInicio para ver dados de outros | mitigate | dataInicio só editável por admin; validação server-side |
| T-11-08 | Information Disclosure | Admin esconde inativos para não ver dados | mitigate | Toggle é consciente — admin decide; não é segurança |
</threat_model>

<verification>
- [ ] Worker with April dataInicio does NOT see March pending approvals
- [ ] Worker with April dataInicio CANNOT fill hours for March
- [ ] Worker with dataFim passed sees "Acesso Bloqueado" screen
- [ ] Admin sees toggle to show/hide inactive workers
- [ ] Admin sees count of inactive workers even when filtered out
- [ ] Reports/costs only include hours from months worker was active
</verification>

<success_criteria>
1. Worker that started in April does NOT see notifications for March
2. Worker that started in April CANNOT fill hours for March
3. If dataFim exists and passed, worker sees "Acesso Bloqueado"
4. Admin has toggle to show/hide inactive workers with count indicator
5. Reports and calculations only include hours from months the worker was active

After completion, create `.planning/phases/11-gestao-trabalhadores/11-04-SUMMARY.md`
</success_criteria>

<output>
Create .planning/phases/11-gestao-trabalhadores/11-04-SUMMARY.md after execution
</output>
