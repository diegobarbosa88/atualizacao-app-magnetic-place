---
phase: 10
plan: 02
type: execute
wave: 1
depends_on: [10-01]
files_modified: []
autonomous: true
requirements: [CLEANUP-01]
user_setup: []

must_haves:
  truths:
    - "LoginView extracted to src/features/auth/LoginView.jsx"
    - "FinancialReportOverlay extracted to src/features/admin/FinancialReportOverlay.jsx"
    - "DocumentsAdmin extracted to src/features/admin/DocumentsAdmin.jsx"
    - "NotificationsAdmin extracted to src/features/admin/NotificationsAdmin.jsx"
  artifacts:
    - path: "src/features/auth/LoginView.jsx"
      provides: "Login screen component"
      min_lines: 165
    - path: "src/features/admin/FinancialReportOverlay.jsx"
      provides: "Financial analysis overlay with AI insights"
      min_lines: 70
    - path: "src/features/admin/DocumentsAdmin.jsx"
      provides: "Admin document management interface"
      min_lines: 300
    - path: "src/features/admin/NotificationsAdmin.jsx"
      provides: "Admin notification/banner management"
      min_lines: 270
  key_links:
    - from: "src/app.jsx"
      to: "src/features/auth/LoginView.jsx"
      via: "import"
      pattern: "import.*LoginView.*from.*features/auth"
    - from: "src/app.jsx"
      to: "src/features/admin/DocumentsAdmin.jsx"
      via: "import"
      pattern: "import.*DocumentsAdmin.*from.*features/admin"
---

<objective>
Extract feature-specific components to their respective feature directories. LoginView goes to auth feature, while admin components (FinancialReportOverlay, DocumentsAdmin, NotificationsAdmin) go to admin feature.

Purpose: Organize components by domain, prepare for pure router refactor
Output: 4 new files in src/features/
</objective>

<execution_context>
@C:/Users/diego/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/app.jsx (lines 165-235 FinancialReportOverlay, 1281-1581 DocumentsAdmin, 1586-1858 NotificationsAdmin, 1860-2026 LoginView)
@src/features/admin/index.js (existing feature exports)
@src/features/worker/WorkerDashboard.jsx (WorkerContext usage pattern to follow)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract LoginView to src/features/auth/LoginView.jsx</name>
  <files>src/features/auth/LoginView.jsx</files>
  <action>
Create directory `src/features/auth/` if it doesn't exist.

Extract `LoginView` component from app.jsx lines 1860-2026 to `src/features/auth/LoginView.jsx`.

The LoginView component signature is:
```jsx
const LoginView = ({ workers, onLogin, systemSettings, setSystemSettings }) => {
```

Key dependencies:
- Uses `useState`, `useEffect` from React
- Uses lucide-react icons: UserCircle, Lock, Download, AlertCircle, ExternalLink, X
- Uses CompanyLogo component (will import from common after Plan 01)
- Has inline styles for bounce-subtle animation
- Handles PWA installation prompts (iOS and Android)

Create the file with proper React imports and export.
</action>
  <verify>
File exists at src/features/auth/LoginView.jsx with LoginView component exported. `grep -c "export.*LoginView\|export default LoginView" src/features/auth/LoginView.jsx` returns 1 or more.
</verify>
  <done>LoginView extracted to src/features/auth/LoginView.jsx</done>
</task>

<task type="auto">
  <name>Task 2: Extract FinancialReportOverlay to src/features/admin/</name>
  <files>src/features/admin/FinancialReportOverlay.jsx</files>
  <action>
Extract `FinancialReportOverlay` component from app.jsx lines 165-235 to `src/features/admin/FinancialReportOverlay.jsx`.

The component signature:
```jsx
const FinancialReportOverlay = ({ logs, workers, clients, expenses, finFilter, setFinFilter, setShowFinReport }) => {
```

Key dependencies:
- Uses `useState`, `useMemo` from React
- Uses lucide-react icons: X, Wallet, BrainCircuit, Sparkles
- Uses CompanyLogo (will import from common after Plan 01)
- Uses `formatCurrency` from formatUtils
- Uses `callGemini` from aiUtils

The component:
1. Displays revenue, team costs, and net profit stats
2. Has an AI-powered insight generator using Gemini
3. Uses FinancialReportOverlay CSS from inline styles in app.jsx (those go away - the overlay itself handles the modal)
</action>
  <verify>
File exists at src/features/admin/FinancialReportOverlay.jsx with FinancialReportOverlay component exported. `grep -c "export.*FinancialReportOverlay\|export default FinancialReportOverlay" src/features/admin/FinancialReportOverlay.jsx` returns 1 or more.
</verify>
  <done>FinancialReportOverlay extracted to src/features/admin/FinancialReportOverlay.jsx</done>
</task>

<task type="auto">
  <name>Task 3: Extract DocumentsAdmin to src/features/admin/DocumentsAdmin.jsx</name>
  <files>src/features/admin/DocumentsAdmin.jsx</files>
  <action>
Extract `DocumentsAdmin` component from app.jsx lines 1281-1581 to `src/features/admin/DocumentsAdmin.jsx`.

The component signature:
```jsx
const DocumentsAdmin = ({ workers = [], documents = [], setDocuments }) => {
```

Key dependencies:
- Uses `useState` from React
- Uses lucide-react icons: FileText, Eye, CheckCircle, Trash2, Search, Upload, Loader2
- Uses `formatDocDate` from dateUtils
- Uses supabase from AppContext (via window.supabase or passed as global)

Features:
1. Document upload with worker/tipo selection
2. Search and filter functionality
3. Sortable table of documents
4. View original/signed PDF, delete actions
</action>
  <verify>
File exists at src/features/admin/DocumentsAdmin.jsx with DocumentsAdmin component exported. `grep -c "export.*DocumentsAdmin\|export default DocumentsAdmin" src/features/admin/DocumentsAdmin.jsx` returns 1 or more.
</verify>
  <done>DocumentsAdmin extracted to src/features/admin/DocumentsAdmin.jsx</done>
</task>

<task type="auto">
  <name>Task 4: Extract NotificationsAdmin to src/features/admin/NotificationsAdmin.jsx</name>
  <files>src/features/admin/NotificationsAdmin.jsx</files>
  <action>
Extract `NotificationsAdmin` component from app.jsx lines 1586-1858 to `src/features/admin/NotificationsAdmin.jsx`.

The component signature:
```jsx
const NotificationsAdmin = ({ workers, appNotifications, saveToDb, handleDelete }) => {
```

Key dependencies:
- Uses `useState` from React
- Uses lucide-react icons: Megaphone, Bell, Loader2, Plus, Trash2, X
- Uses supabase from AppContext

Features:
1. Create new banner notifications with title, message, type, target
2. View existing notifications with viewed/dismissed counts
3. Toggle active status
4. Delete notifications
5. View detailed interaction modal
</action>
  <verify>
File exists at src/features/admin/NotificationsAdmin.jsx with NotificationsAdmin component exported. `grep -c "export.*NotificationsAdmin\|export default NotificationsAdmin" src/features/admin/NotificationsAdmin.jsx` returns 1 or more.
</verify>
  <done>NotificationsAdmin extracted to src/features/admin/NotificationsAdmin.jsx</done>
</task>

</tasks>

<verification>
All four feature components exist in their respective directories and are ready to be imported by app.jsx after Plan 03 updates imports.
</verification>

<success_criteria>
1. `src/features/auth/LoginView.jsx` exists with default export
2. `src/features/admin/FinancialReportOverlay.jsx` exists with default export
3. `src/features/admin/DocumentsAdmin.jsx` exists with default export  
4. `src/features/admin/NotificationsAdmin.jsx` exists with default export
5. All feature index files updated to export new components
</success_criteria>

<output>
After completion, create `.planning/phases/10-limpeza-roteamento/10-02-SUMMARY.md`
</output>
