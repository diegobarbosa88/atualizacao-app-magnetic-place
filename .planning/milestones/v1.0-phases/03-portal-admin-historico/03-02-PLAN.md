---
phase: 03-portal-admin-historico
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.jsx
autonomous: true
requirements:
  - PORTAL-02

must_haves:
  truths:
    - "Admin can search reports by text in client name"
    - "Search updates results in real-time"
    - "Search combines with existing filters"
  artifacts:
    - path: "src/app.jsx"
      provides: "Text search input for report history"
      contains: "searchText|searchReports"
---

<objective>
Add text search capability to the admin report history view.
Purpose: Allow admin to quickly find specific reports by searching client name or other text fields.
Output: Search input that filters the visible reports in real-time.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-portal-admin-historico/03-01-PLAN.md
</context>

<interfaces>
<!-- Key code patterns from existing implementation -->

From 03-01-PLAN.md - Filter state variables:
```javascript
const [filterClient, setFilterClient] = useState('');
const [filterStatus, setFilterStatus] = useState('');
const [filterMonth, setFilterMonth] = useState('');
```

From src/app.jsx - Client name lookup pattern:
```javascript
const cliente = clients.find(c => c.id === clientId);
```

From src/app.jsx - Input pattern for text search:
```jsx
<input 
  type="text" 
  placeholder="Pesquisar..."
  value={searchText}
  onChange={(e) => setSearchText(e.target.value)}
  className="px-3 py-2 border rounded-lg text-sm"
/>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add search state variable</name>
  <files>src/app.jsx</files>
  <read_first>
    - src/app.jsx (near line 1173 for filter state variables from 03-01-PLAN)
  </read_first>
  <action>
    Add search state variable after the existing filter state variables.
    
    Add this near the filter state variables (around line 1175):
    ```javascript
    const [searchText, setSearchText] = useState('');
    ```
    
    This will be used to filter reports by client name or other text fields.
  </action>
  <verify>
    <automated>grep -c "searchText.*useState" src/app.jsx</automated>
  </verify>
  <done>Search state variable added</done>
</task>

<task type="auto">
  <name>Task 2: Add search input to filter controls</name>
  <files>src/app.jsx</files>
  <read_first>
    - src/app.jsx (around line 1355 for filter controls from 03-01-PLAN)
  </read_first>
  <action>
    Add a search input to the existing filter controls bar.
    
    Find the filter controls created in 03-01-PLAN (the div with className="flex gap-2 mb-4 p-3 bg-slate-50 rounded-xl").
    
    Add the search input as the first element in the filter bar:
    ```jsx
    {/* Search Input */}
    <div className="relative flex-1">
      <input 
        type="text" 
        placeholder="Pesquisar por cliente..." 
        value={searchText}
        onChange={(e) => setSearchText(e.target.value)}
        className="w-full px-3 py-2 pl-9 border rounded-lg text-sm"
      />
      <svg className="absolute left-3 top-2.5 w-4 h-4 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
      </svg>
    </div>
    ```
    
    Place this BEFORE the client dropdown filter.
  </action>
  <verify>
    <automated>grep -c "Pesquisar por cliente\|searchText" src/app.jsx</automated>
  </verify>
  <done>Search input added to filter controls</done>
</task>

<task type="auto">
  <name>Task 3: Apply text search to history list</name>
  <files>src/app.jsx</files>
  <read_first>
    - src/app.jsx (around line 1205-1225 for historico computation from 03-01-PLAN)
  </read_first>
  <action>
    Update the historico filtering logic to include text search.
    
    Find the filtering code added in 03-01-PLAN Task 3. Add text search filtering after the month filter:
    
    ```javascript
    // Apply text search (client name)
    if (searchText) {
      const searchLower = searchText.toLowerCase();
      historico = historico.filter(d => {
        const client = clients?.find(c => c.id === d.clientId);
        const clientName = client?.name?.toLowerCase() || '';
        return clientName.includes(searchLower);
      });
    }
    ```
    
    This filters the history list to only show reports where the client name contains the search text.
  </action>
  <verify>
    <automated>grep -c "searchText.*toLowerCase\|clientName.*includes" src/app.jsx</automated>
  </verify>
  <done>Text search filters history list by client name</done>
</task>

</tasks>

<verification>
1. grep "searchText" src/app.jsx returns 3+ (state + input + filter)
2. Search input appears in the filter bar
3. Typing in search input filters the visible reports
4. Search combines with other filters (client, status, month)
</verification>

<success_criteria>
- Admin can type in search box to find reports by client name
- Search updates results in real-time as user types
- Search works together with existing filters
- Empty search shows all filtered results
</success_criteria>

<output>
After completion, create `.planning/phases/03-portal-admin-historico/03-02-SUMMARY.md`
</output>