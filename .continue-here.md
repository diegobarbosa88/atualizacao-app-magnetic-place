# Continue Here — Phase 19: Painel Tempo Real + GPS

**Pausado em:** 2026-05-21  
**Motivo:** Contexto esgotado (75%) durante implementação

---

## O que está a ser implementado

**Phase 19 — Painel em Tempo Real no Portal do Cliente + Auto-Registo por Geolocalização**

Plano completo em: `C:\Users\diego\.claude\plans\scalable-sniffing-muffin.md`

### Objectivo
1. **Portal do Cliente** → nova tab "Hoje" com registos dos trabalhadores em tempo real
2. **Portal do Trabalhador** → GPS check ao guardar registo; verifica se está dentro do raio da unidade
3. **Admin** → configurar coordenadas GPS e raio de cada unidade (geofence)

---

## Estado das Tarefas

| # | Tarefa | Estado |
|---|--------|--------|
| 1 | DB migration — colunas GPS em `clients` e `logs` | ⏳ Pendente (SQL abaixo) |
| 2 | `src/utils/geoUtils.js` — haversine + geofence | ✅ **Feito** |
| 3 | `AppContext.jsx` — GPS fields no mapping de `logs` em `saveToDb` | ⏳ Pendente |
| 4 | `ClientManager.jsx` — UI configurar geofence por unidade | ⏳ Pendente |
| 5 | `WorkerDashboard.jsx` — GPS check ao guardar registo | ⏳ Pendente |
| 6 | `ClientPortal.jsx` — tab "Hoje" com painel tempo real + badges GPS | ⏳ Pendente |

---

## Próximas Acções

### Tarefa 1 — SQL (correr no Supabase dashboard)
```sql
ALTER TABLE clients ADD COLUMN IF NOT EXISTS lat FLOAT;
ALTER TABLE clients ADD COLUMN IF NOT EXISTS lng FLOAT;
ALTER TABLE clients ADD COLUMN IF NOT EXISTS geo_radius_m INT DEFAULT 200;
ALTER TABLE logs ADD COLUMN IF NOT EXISTS check_in_lat FLOAT;
ALTER TABLE logs ADD COLUMN IF NOT EXISTS check_in_lng FLOAT;
ALTER TABLE logs ADD COLUMN IF NOT EXISTS geo_verified BOOLEAN;
```

### Tarefa 3 — AppContext.jsx linhas 355-366
Adicionar ao payload de logs em `saveToDb`:
```js
check_in_lat: data.check_in_lat ?? null,
check_in_lng: data.check_in_lng ?? null,
geo_verified: data.geo_verified ?? null,
```

### Tarefa 4 — ClientManager.jsx
Na UI de edição de cliente, secção "Geolocalização":
- Inputs: `lat`, `lng`, `geo_radius_m` (default 200m)
- Botão "Usar localização atual" → `getCurrentPosition()` de `geoUtils.js`

### Tarefa 5 — WorkerDashboard.jsx
Wrap do submit do form (inline + main):
1. `await getCurrentPosition()` → `{lat, lng}`
2. `isWithinGeofence(lat, lng, client.lat, client.lng, client.geo_radius_m)`
3. Se fora: confirmar override com distância
4. Adicionar `check_in_lat, check_in_lng, geo_verified` ao formData antes de chamar `handleSaveEntry`

Import: `import { getCurrentPosition, isWithinGeofence, distanceMeters } from '../../utils/geoUtils'`

### Tarefa 6 — ClientPortal.jsx
- Nova tab "Hoje" no header (junto ao currentView)
- Novo bloco `currentView === 'hoje'` → componente PainelHoje
- PainelHoje: filtra `logs` por `date === hoje` + `clientId`, agrupa por worker, mostra badges GPS
- Real-time já funciona via canal `client-portal-logs` linhas 67-95 (não duplicar)

---

## Ficheiros Chave

| Ficheiro | Nota |
|---|---|
| `src/utils/geoUtils.js` | ✅ Criado — haversine, isWithinGeofence, getCurrentPosition |
| `src/ClientPortal.jsx` | Tab + painel; canal real-time já nas linhas 67-95 |
| `src/features/worker/WorkerDashboard.jsx` | GPS check no submit |
| `src/features/worker/contexts/WorkerContext.jsx` | handleQuickRegister linha 167 |
| `src/features/admin/ClientManager.jsx` | Formulário geofence |
| `src/context/AppContext.jsx` | saveToDb logs mapping linhas 355-366 |

## Padrões a Seguir
- `calculateHoursDiff` já existe em ClientPortal.jsx linha 10 — reutilizar
- GPS badge colors: `bg-emerald-100 text-emerald-700` / `bg-slate-100 text-slate-500` / `bg-rose-100 text-rose-700`
- Estilos portal: `rounded-[2.5rem]`, `shadow-xl`, TailwindCSS
