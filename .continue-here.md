# Continue Here
Generated: 2026-05-21T~03:00Z

## Position
Ad-hoc fixes session — sem fase formal activa. Todas as alterações deployadas em produção (app-magnetic.vercel.app).

## Completed This Session
1. **Admin precision correction view** — usa `StepPrecision` pré-preenchido com alterações do cliente; admin pode modificar antes de aplicar (commit `1ae5d58`)
2. **Status check faturas reconciliadas** — coluna Estado (✓ Pago / Pendente) na aba Faturas + pills filtro Todas/Pendentes/Reconciliadas + botão verde "Reconciliadas" + PDF melhorado com colunas Estado e Ficheiro (commit `bc3fb85`)
3. **Fix FaturasAdmin blank page** — `CheckCircle` não estava importado; também corrigido handler `gerarPDF` que recebia event como argumento (commit `d6b1609`)
4. **Fix notificações após hard refresh** — `myNotifications` agora verifica `dismissed_by_ids` da BD além de localStorage; admin não vê notificações `target_worker_ids` (commit `2fbc837`)
5. **Checkbox em transferências client_association** — removida exclusão na condição da checkbox da aba Reconciliadas (commit `bb5844c`)
6. **Fix erro UUID ao confirmar** — `confirmarPagamento` e `confirmarBulkMatched` protegidos contra `fatura: null` em itens `client_association` (commits `b5b8a3f`, `5d88f59`)
7. **Badge "Associado" + bulk confirm corrigido** — itens `client_association` mostram badge verde "Associado"; `confirmarBulkMatched` usa array de display combinado para índices correctos com mensagem se nada confirmável (commit `f74d3e1`)

## Remaining Work
Nenhuma tarefa pendente conhecida. Próximas prioridades potenciais:
- Phase 14: editor de blocos JSON (planeado mas não iniciado)
- Quaisquer novos pedidos do utilizador

## Blockers
Nenhum.

## Key Decisions
- `StepPrecision` aceita `initialEntries` prop para pré-popular com dados do cliente
- `confirmarBulkMatched` usa `[...displayData.matched, ...clientAssocMatched]` para mapping correcto de índices
- Notificações `target_worker_ids` nunca mostradas a admins (`currentUser.role !== 'admin'`)
- `dismissed_by_ids` da BD verificado em `myNotifications` para persistir dismissals após hard refresh

## Uncommitted Files
- `.planning/STATE.md` (minor state drift)
- `public/version.json` (version bump from deploys)

## Resume Instructions
Corre `/gsd-resume-work` na próxima sessão.
